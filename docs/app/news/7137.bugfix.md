Fix Edit this page links on generated API and Cloud CLI references to open their source files, and support source branches in docs previews.

Simplify the docs sidebar with aligned text-only navigation and expansion arrows.

Remove the Custom Components promotional card from the component-library sidebar.

Show single-page sidebar groups as direct links with a neutral selected highlight and no redundant dropdown.

Preserve sidebar scroll position when selecting a page instead of recentering the selected link.

Standardize generated API and environment-variable references with compact field metadata, detailed method sections, and member navigation. List API reference pages directly in the sidebar without an accordion.
