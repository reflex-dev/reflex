Import `mergician` without a version specifier so it collapses onto the base dependency during package collection, leaving `PackageJson.DEPENDENCIES` as the single owner of the pin. The `reflex-base` floor moves up accordingly: the unversioned import only resolves to a pinned version against a `reflex-base` that declares one.
