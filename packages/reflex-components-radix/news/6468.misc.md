Updated `RadixThemesComponent`, `IconButton`, and `TextArea` to apply their setup with `copy_with(...)` and non-mutating style handling instead of mutating component instances after creation, matching the immutable component model. No change to public APIs or rendered output.
