`DataEditor` now derives its `get_cell_content` callback name deterministically from its `columns`/`data` and wires it via `copy_with(...)` at creation, with `add_hooks` emitting the matching hook rather than mutating the component in place, matching the immutable component model. No change to public APIs.
