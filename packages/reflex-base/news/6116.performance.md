The JSON helpers in `reflex_base.utils.format` use orjson when the new `reflex-base[orjson]` extra is installed, falling back to the stdlib for payloads orjson cannot represent faithfully. Output is identical with and without the extra, except floats below 1e-4 may be rendered differently (`0.00001` versus `1e-05`).
