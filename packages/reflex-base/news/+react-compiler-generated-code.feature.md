React Compiler now optimizes generated components that use `id` refs, generated UUIDs, and memoized event handlers, and the `react_compiler` option now installs the experimental `babel-plugin-react-compiler` release. Component `id` refs register through the new `useRegisteredRef` hook in `$/utils/state`.
