`create_passthrough_component_memo` evaluates the memo body once per definition, reuses previously built definitions via the new `existing_definitions` parameter, and shares one import-time params analysis across all passthrough wrappers.
