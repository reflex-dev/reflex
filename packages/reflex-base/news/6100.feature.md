`RegistrationContext` supports pluggable `NameResolver`s so the compiler and event pipeline can emit minified state and event handler names, opted in via the new `REFLEX_MINIFY_STATES`/`REFLEX_MINIFY_EVENTS` env vars.
