Add inert OpenTelemetry trace points and metrics around event handler execution, state acquisition, socket messages and compile stages (`reflex_base.otel`) and `window.__reflex_otel` hooks in the frontend event loop and upload helper; they cost one boolean check until the `reflex-otel` package enables them.
