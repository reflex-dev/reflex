Component content hashing, which auto-memoization runs for every memoized component during a compile, encodes about 1.5x faster on large pages. Generated memo module names change as a result; nothing outside the compiled output refers to them.
