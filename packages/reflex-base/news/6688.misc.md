Added an optional per-page source-read recorder hook (`page_source_recorder` in the compiler plugin) used by the incremental compile cache to track the exact files each page reads during evaluation. Auto-generated unique ref names are now seeded per page from its route (and the page's sequence is resumed for its render), so a page's generated names are reproducible and independent of which other pages compile alongside it. No behavior change unless the compile cache is enabled.
