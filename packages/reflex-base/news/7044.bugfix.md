`rx.Config` now rejects a `frontend_path` whose segments are not plain directory names (`..`, backslashes, or drive letters), which would otherwise relocate the production build output outside of `.web/build/client`.
