Components are now frozen after `Component.create()`. Mutating a component instance in place (assigning to attributes such as `style`, `children`, or `event_triggers`) is no longer supported; use `copy_with(...)` to derive a modified copy instead.
