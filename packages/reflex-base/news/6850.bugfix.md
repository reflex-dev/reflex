Compiler-generated memo wrappers with a tagged root now forward props and refs their parent injects at runtime to that root, following Radix `Slot` semantics via the new `mergeSlotProps` and `mergeRefs` helpers in `$/utils/state`. `mergician` is now a base frontend dependency.
