Give each substate its own React context provider in the generated `utils/context.js`. A state delta now re-renders only the provider for the substate it touched, instead of recreating every `StateContexts` element under a single combined provider.
