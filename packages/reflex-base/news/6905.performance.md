Dev mode no longer pays for React's per-element owner-stack capture: navigation clicks in a large app dropped from ~350ms to ~83ms of main-thread CPU (5.6x prod down to ~1.3x). Set `REFLEX_REACT_OWNER_STACKS=1` to restore owner-stack detail in React DevTools.
