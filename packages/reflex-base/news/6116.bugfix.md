`orjson_dumps` raises `TypeError` for unserializable values instead of writing `null`, and escapes strings that are not valid UTF-8. A serializer registered under a union annotation (for example `MyEnum | None`) disables orjson's native handling like one registered under the bare type.
