Qualify annotations whose bare builtin name is shadowed by a member of the same class, so `Var.create` is no longer inferred as `LiteralBooleanVar` for every argument type. `Var.bool`, `BaseComponent.set` and `PropsBase.dict` shadowed `bool`, `set` and `dict` for annotations elsewhere in their own class bodies, which type checkers resolve against the class namespace.
