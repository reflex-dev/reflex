`reflex_base.config._load_config()` is deprecated and will be removed in 1.0. It now warns and delegates to `_get_config()`, which does the loading and takes an optional `project_root` to load a config from a directory other than the cwd. Use `get_config()` to read the config cached on the current `RegistrationContext`.
