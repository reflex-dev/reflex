`RegistrationContext` now carries the loaded `Config`, the registered `App`, decorated pages, and bundled libraries, and provides `fork()` to derive a fresh context that preserves existing registrations while resetting the app and config.
