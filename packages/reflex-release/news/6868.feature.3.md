GitHub releases for the root package are now titled with their tag (`v0.2.1`) rather than `<package>@<version>`. The root package is the repository, so its tag already names the release unambiguously; sub-package releases still carry the package name.
