The `SHA256SUMS` manifest attached to a GitHub release now lists the distribution files by bare filename, covering exactly what was uploaded to PyPI. It previously carried `dist/`-prefixed paths and an entry for the release notes, so `sha256sum -c SHA256SUMS` against downloaded release assets reported missing files instead of verifying them.
