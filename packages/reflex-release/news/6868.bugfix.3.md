The pull-request guard that rejects hand-written changelog version headings now runs on pull requests into every branch the release workflow publishes from, not only the main branch. A merged version heading is a publish trigger wherever it lands, and hotfix branches publish final versions without going through the main branch at all.
