The release commit now stages only the changelogs of the packages in the release, instead of every changelog present in the repository. A local `materialize` in a worktree that carried an unrelated changelog edit would otherwise have swept it into the release commit.
