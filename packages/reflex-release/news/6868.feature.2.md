The Dispatch release job summary now links what it produced instead of only naming it: the release pull request as `#<number>`, the prerelease branch and its release-workflow runs as URLs. Each is also emitted as a notice annotation, so it appears on the run page itself rather than only in the summary tab.
