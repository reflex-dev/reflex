The `SHA256SUMS` manifest is attached to the GitHub release, so the record of what a published version contains outlives the workflow artifact it was built as. Anyone can verify a downloaded wheel against the release with `sha256sum -c SHA256SUMS`.
