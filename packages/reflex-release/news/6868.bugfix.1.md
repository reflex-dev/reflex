A towncrier `title_format` that writes no version heading is now rejected instead of silently stranding every release. `title_format = false` tells towncrier to omit the heading, and the pipeline finds versions by reading those headings — so a repository configured that way would have materialized changelogs no release could ever be detected from. A non-string `title_format` is rejected the same way rather than raising a traceback.
