`reflex deploy` now consistently shows the "Built with Reflex" badge on apps deployed on the free tier, whichever reflex version builds them. Paid plans can still hide it with `show_built_with_reflex=False`.
