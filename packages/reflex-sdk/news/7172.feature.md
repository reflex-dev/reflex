Deploy apps with `reflex-sdk`: `client.deployments.create` streams a build's archives to storage and submits the deployment, and `client.deployments.wait` follows it until it goes live, raising `DeploymentFailedError` with the failure report otherwise. The client also reads deployment status, reports and build logs, lists regions and machine sizes, and reserves hostnames with `client.apps.reserve_hostname`.
