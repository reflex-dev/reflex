`DataTable._render` now derives its `columns`/`data` via `remove_props`/`add_props` rather than mutating the component in place, matching the immutable component model. No change to public APIs or rendered output.
