Reworked `Bare`, `DebounceInput`, `Foreach`, `Upload`, `BackendDisabled`, and the window-event components to assemble their output with `copy_with(...)` and other non-mutating patterns instead of mutating component instances after creation, matching the immutable component model. No change to public APIs or rendered output.
