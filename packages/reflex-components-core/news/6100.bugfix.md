`REFLEX_PERF_MODE` detects a state `Var` rendered into a plain string by its trailing field marker rather than by the built-in state name prefix, so it no longer flags unrelated strings that merely start with `reflex___state`, and keeps working when state names are minified.
