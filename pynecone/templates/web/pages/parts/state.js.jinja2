const [{{synced_state}}, {{synced_state|react_setter}}] = useState({{initial_state|json_dumps}})
const [{{result_state}}, {{result_state|react_setter}}] = useState({{initial_result|json_dumps}})
const {{router}} = useRouter()
const socket = useRef(null)
const { isReady } = {{router}}
const { {{color_mode}}, {{toggle_color_mode}} } = {{use_color_mode}}()
