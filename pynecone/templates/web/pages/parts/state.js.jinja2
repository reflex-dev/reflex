const [{{synced_state}}, set{{synced_state.capitalize()}}] = useState({{initial_state}})
const [{{result_state}}, set{{result_state.capitalize()}}] = useState({{initial_result}})
const {{router}} = useRouter()
const socket = useRef(null)
const { isReady } = {{router}}
const { {{color_mode}}, {{toggle_color_mode}} } = {{use_color_mode}}()
