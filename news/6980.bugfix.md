`reflex run` now exits promptly on SIGTERM/SIGINT when started without a TTY (CI jobs, containers, process supervisors): when the backend stops, the frontend dev server process group is terminated so the launcher no longer hangs until SIGKILL.
