`reflex run` no longer reports "Starting frontend failed with exit code 143" and exits non-zero when the frontend child is stopped by SIGTERM. A SIGTERM teardown is now accepted alongside SIGINT, in both the direct (`-15`) and shell-reported (`143`) forms.
