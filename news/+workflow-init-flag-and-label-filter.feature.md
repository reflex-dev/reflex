`reflex init --workflow` scaffolds a workflow-only module — one runnable file, no frontend, no `rxconfig.py` — the same scaffold `reflex workflows init` writes, so the two spellings cannot drift. The console's Runs page gained a label filter: type `tenant=acme region=eu` and only runs carrying every pair are listed, the same `key=value` shape the CLI's `--label` takes; each row now shows its labels.
