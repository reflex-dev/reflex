Refresh the `docker-example` deployments for current Reflex: the two-port example becomes `simple-one-process` (prod mode now serves the frontend and backend on one port), the multi-stage images drop `reflex init`, bun downloads, and `libpq-dev` where the backend never needs them, uv and bun come from their official images, `STOPSIGNAL SIGKILL` is removed now that the backend shuts down cleanly on SIGTERM, Caddy serves the pre-compressed assets Reflex already writes, and the compose example persists Caddy's TLS state in the right directory. The self-hosting docs are updated to match.
