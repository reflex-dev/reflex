A `rx.parallel` join that spans the upgrade which taught arrivals to carry their branch index now still returns results in declaration order. Arrivals recorded by the older release carry no index, so it is recovered from the branch's admission key, which every release has written. An arrival that cannot be identified at all keeps its position rather than being reordered on a guess.
