Explicit state and event id minification: a new `reflex minify` CLI (`init`, `sync`, `validate`, `list`, `lookup`) manages stable short ids for states and event handlers in a checked-in `minify.json`, enabled at compile time via `REFLEX_MINIFY_STATES`/`REFLEX_MINIFY_EVENTS`.
