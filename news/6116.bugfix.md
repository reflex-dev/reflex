Integers in inbound socket events (snowflakes, large database ids) reach event handlers exactly instead of being rounded to floats. Reflex no longer patches the global socket.io packet codec, so other socket.io servers and clients in the same process keep their own JSON settings. `update_package_json_overrides` no longer replaces a `package.json` whose `overrides` is not an object.
