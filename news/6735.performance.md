Auto-memoize compile pass no longer rebuilds duplicate memo definitions: repeated identical stateful subtrees reuse the first definition, the memo body is evaluated once per definition, and the passthrough signature analysis is computed once per process (~20% faster stateful-page compiles).
