The reflex CLI and runtime now log through standard python `logging` per-module loggers, and a new `--json` CLI flag (equivalent to `REFLEX_LOG_JSON`) emits machine-readable JSON-lines records for agent consumption.
