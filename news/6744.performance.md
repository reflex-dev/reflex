Reduce per-event websocket overhead: client headers are decoded once per connection instead of per event, route resolution is cached, outgoing updates are JSON-encoded with orjson (with a stdlib fallback that preserves NaN/Infinity tokens and >64-bit integers), and chained-event routing no longer runs while holding the state lock.
