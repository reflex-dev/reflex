Reduce per-event websocket overhead: client headers are decoded once per connection instead of per event, route resolution is cached, outgoing updates are JSON-encoded with orjson (with a stdlib fallback), and the final delta emit no longer runs while holding the state lock.
