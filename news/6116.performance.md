JSON serialization for state updates over the websocket and JSON file handling now use orjson when available. Install with the new `reflex[orjson]` extra to enable; without it Reflex falls back to the stdlib `json` module.
