JSON serialization for state updates over the websocket and for generated frontend files now uses orjson when available. Install with the new `reflex[orjson]` extra to enable; without it Reflex falls back to the stdlib `json` module.
