JSON serialization for state updates over the websocket and for generated frontend files now uses orjson when available. Install with the new `reflex[orjson]` extra to enable; without it Reflex falls back to the stdlib `json` module. The extra is declared on `reflex-base`, which owns the helpers, so `reflex-base[orjson]` works for a direct dependent too.
