Components produced by the compiler are now immutable. Code that mutated component instances after creation (for example during page compilation or in custom plugins) must switch to `copy_with(...)` to produce updated copies rather than modifying them in place.
