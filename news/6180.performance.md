Memoize the compiled `ThemeProvider` and `EventLoopProvider` so context consumers stop re-rendering on unrelated updates: `setColorMode`/`toggleColorMode` keep a stable identity, the `ThemeContext` and `ColorModeContext` values and the rendered subtrees are memoized, the system color scheme `matchMedia` listener is no longer torn down and re-attached on every render, and the event loop provider element is no longer rebuilt on every navigation.
