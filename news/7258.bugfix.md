Fail with a clear error when a package on the app module path has no `__init__.py`, instead of starting a backend that loads the app under a different module name than the compiled frontend and silently drops every state update.
