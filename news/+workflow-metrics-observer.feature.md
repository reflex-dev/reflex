`rx.workflow.MetricsObserver` tallies the run and attempt counters a deployment alerts on, in total and per workflow, so a metrics endpoint or OpenTelemetry exporter is a few lines over `snapshot()`.
