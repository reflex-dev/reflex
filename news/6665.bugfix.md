Raise minimum dependency versions to pull in security fixes: `starlette>=1.3.1` (Host-header path poisoning, `request.form()` DoS, and UNC-path SSRF), `python-multipart>=0.0.30` (quadratic-time querystring DoS), and `granian>=2.7.4` (WSGI and WebSocket header-panic DoS).
