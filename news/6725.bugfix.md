Event handlers and computed vars copied from a state mixin now preserve all attributes of the source function's `__dict__` (not just the background-task and event-action markers), so custom decorator attributes survive mixin inheritance. Keyword-only default arguments (`__kwdefaults__`) are also preserved, fixing a `TypeError` when calling a copied handler that relied on them.
