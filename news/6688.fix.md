Fixed repeated full recompilation when the incremental compile cache encounters state defaults requiring Reflex serialization, such as Pydantic models or dates. Manifest write failures now produce visible warnings instead of silently disabling cache reuse.
