Set `REFLEX_REDIS_MAX_CONNECTIONS` to cap the Redis connection pool used by the state manager and token manager. Once the cap is reached, requests wait up to `REFLEX_REDIS_POOL_TIMEOUT` (default 20s) for a free connection instead of opening new ones.
