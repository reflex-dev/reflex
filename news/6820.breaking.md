Granian is now always the default backend server. The legacy Uvicorn/Gunicorn backend is fully opt-in: it is only used when `REFLEX_USE_GRANIAN=0` is explicitly set, and Reflex exits with an actionable error if its required packages are not installed.
