Replace the runtime HTTP dependency `httpx` with `httpx2` (the actively maintained fork). TLS verification now uses the OS trust store instead of the bundled `certifi` CA bundle; deployments behind corporate proxies or in minimal containers may need `SSL_CERT_FILE` / `SSL_CERT_DIR` set.
