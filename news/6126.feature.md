Add `rx.get_token_manager()` with lifecycle helpers (`session_is_connected`, `token_is_connected`, `when_session_disconnects`, `when_token_disconnects`) so background events can stop work when the client disconnects.
