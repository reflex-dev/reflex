A `reflex run` whose install would use npm - whether via `REFLEX_USE_NPM=1` or because bun is unavailable - and whose node version fails the preflight no longer persists npm lockfile state into `reflex.lock/`: the version check now gates the selected npm install itself, so a failed run leaves the project's bun lockfiles untouched instead of silently switching later runs to npm.
