A `reflex run` under `REFLEX_USE_NPM=1` that fails the node-version preflight no longer persists npm lockfile state into `reflex.lock/`: the version check now gates the npm install itself, so a failed run leaves the project's bun lockfiles untouched instead of silently switching later runs to npm.
