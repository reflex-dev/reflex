The root state gained five reserved base vars holding the router data: `rx_router_session`, `rx_router_headers`, `rx_router_page`, `rx_router_url` and `rx_router_route_id`. A state subclass that declares one of these names now raises a clear error and must rename its field. `State.router` itself is unchanged.
