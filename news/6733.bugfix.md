Plugin `post_compile` hooks now run at most once per app instance. Previously every ASGI app construction re-ran the hooks, re-applying app mutations such as `add_middleware` — e.g. installing a duplicate middleware that gated every event twice.
