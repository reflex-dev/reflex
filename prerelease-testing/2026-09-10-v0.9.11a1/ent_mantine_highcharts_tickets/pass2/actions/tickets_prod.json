[
 {"goto": "/"},
 {"wait": 4000},
 {"shot": "prod_t_index"},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "prod: shipped page (dead UI expected)"},
 {"click_text": "Seed"},
 {"wait": 3000},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "prod: Seed does nothing either"},
 {"shot": "prod_t_dead"},
 {"goto": "/ref"},
 {"wait": 4000},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "prod: reference page hydrates"},
 {"click_text": "New Ticket"},
 {"wait": 1000},
 {"type": ["input[placeholder='Title']", "prod ticket"]},
 {"wait": 800},
 {"click_text": "Create"},
 {"wait": 3000},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, titles: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.split('\\n')[0]).slice(0,3), badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "prod: CRUD create works on the hydrating page"},
 {"shot": "prod_t_created"},
 {"click_nth": ["tbody tr button", 0], "note": "prod: Start"},
 {"wait": 2000},
 {"eval": "() => Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.replace(/\\n/g,' | ')).slice(0,2)"},
 {"shot": "prod_t_status"}
]
