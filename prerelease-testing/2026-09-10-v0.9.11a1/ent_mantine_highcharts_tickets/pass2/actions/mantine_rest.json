[
 {"goto": "/"},
 {"wait": 3000},
 {"shot": "index"},
 {"eval": "() => ({cards: Array.from(document.querySelectorAll('.rt-Card')).map(c=>c.innerText.replace(/\\n/g,' / ')), links: Array.from(document.querySelectorAll('a')).map(a=>[a.innerText, a.getAttribute('href')])})", "note": "index lists the registered demos"},
 {"click": "button[role=combobox], .rt-SelectTrigger", "note": "radix select dropdown in the demo header"},
 {"wait": 1000},
 {"eval": "() => Array.from(document.querySelectorAll('[role=option]')).map(e=>[e.innerText, e.getAttribute('data-value')])"},
 {"shot": "index_select_open"},
 {"click": "[role=option]:has-text('Pill')", "note": "select -> on_change=rx.redirect"},
 {"wait": 3000},
 {"eval": "() => ({url: location.pathname, pills: document.querySelectorAll('[class*=mantine-Pill-root]').length, body: document.body.innerText.replace(/\\n/g,' | ').slice(0,160)})", "note": "redirected to /pill"},
 {"shot": "pill_page"},
 {"click": "[class*=Pill-remove]", "note": "pill remove button -> rx.toast('Removed')"},
 {"wait": 1500},
 {"eval": "() => ({toasts: Array.from(document.querySelectorAll('[data-sonner-toast]')).map(e=>e.innerText.replace(/\\n/g,' ')), pills: document.querySelectorAll('[class*=mantine-Pill-root]').length})"},
 {"shot": "pill_removed"},
 {"goto": "/tags-input"},
 {"wait": 3000},
 {"eval": "() => ({tags: Array.from(document.querySelectorAll('[class*=Pill-root]')).map(e=>e.innerText), input: document.querySelectorAll('[class*=TagsInput-input] input, .mantine-TagsInput-inputField').length, body: document.body.innerText.replace(/\\n/g,' | ').slice(0,160)})", "note": "initial state tags Tag1/Tag2"},
 {"shot": "tagsinput_load"},
 {"click": ".mantine-TagsInput-inputField"},
 {"keyboard": "Q"},
 {"keyboard": "A"},
 {"keyboard": "Enter"},
 {"wait": 1500},
 {"eval": "() => Array.from(document.querySelectorAll('[class*=Pill-root]')).map(e=>e.innerText)", "note": "state round trip: QA added"},
 {"shot": "tagsinput_added"},
 {"click_nth": ["[class*=Pill-remove]", 0], "note": "remove Tag1 -> on_change -> state list"},
 {"wait": 1500},
 {"eval": "() => Array.from(document.querySelectorAll('[class*=Pill-root]')).map(e=>e.innerText)"},
 {"reload": null},
 {"wait": 3000},
 {"eval": "() => Array.from(document.querySelectorAll('[class*=Pill-root]')).map(e=>e.innerText)", "note": "after reload the disk-state manager restores the edited tag list"},
 {"shot": "tagsinput_reload"},
 {"goto": "/accordion"},
 {"wait": 2000},
 {"eval": "() => document.body.innerText.slice(0,80)", "note": "route of a commented-out demo module"},
 {"shot": "unregistered_route"}
]
