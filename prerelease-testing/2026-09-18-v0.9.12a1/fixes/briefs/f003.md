# f003 — FINDING-003 / issue #7212: withheld `@rx.var(cache=False)` is never re-sent (HIGH, regression)

Worktree: `/home/user/wt/f003` · Branch: `fix/finding-003-delta-memo` · Ports: 3920-3929 / 8920-8929 (redis, if you use one: 8929)
Campaign material: FINDINGS.md `## FINDING-003`; `event_loop/NOTES.md` ISSUE-1 and its `## VERIFICATION`;
`event_loop/elapp` (page `/filtered`), `event_loop/scripts/s_filtered.py` (+ `scripts/wsdrive.py`),
`event_loop/verification/`; `ent_mcp_oidc/verification/2026-09-19-adversarial/pure_delta_memo.py` (browser-free repro).

Root cause: `BaseState.get_delta` (`reflex/state.py` ~2380-2386) calls `cvar._record_delta_value(...)` /
`_drop_unchanged_delta_value` while BUILDING the delta; `ComputedVar._record_delta_value`
(`packages/reflex-base/src/reflex_base/vars/base.py` ~2689-2723) writes `instance.__last_delta_<js_expr> = (token, key)`
immediately. A downstream `get_delta` wrapper (a documented pattern — see the `get_delta` docstring; reflex-enterprise
0.9.5 auth does it) that removes the entry leaves the memo saying "sent", so the same value is deduped away later.
`_suppress_delta_recording()` (`reflex/state.py` ~317) exists but is private and used only by `reflex/istate/shared.py`.

Fix goal: an uncached var withheld from the delivered delta must be re-sent once it is no longer withheld, while
the #6946 dedupe keeps working for values that WERE delivered (its tests must stay green and its wire savings must
remain). Study every `get_delta` call site (`reflex/state.py`, `reflex/app.py`, `reflex/istate/*`, tests) before
choosing a shape. Candidates: commit the memo only for entries present in the delta that actually leaves the
override chain (e.g. gather candidates during `get_delta`, finalize where the StateUpdate is emitted, or keep a
per-instance pending map the caller confirms); or record after filtering by giving overrides a post-filter hook.
The "downstream overrides `get_delta` with a self-only signature" contract must survive. Explain trade-offs in
REPORT.md. Related campaign observation (mention whether your fix changes it, do not widen scope to fix it unless it
falls out naturally): deltas sent to a disconnected client are recorded as sent, safe only because reconnect
re-hydrates via `dict()`.

Regression test: `tests/units/test_state.py` — a State with a `cache=False` var and a `get_delta` wrapper that drops
the key while a flag is set; after the flag clears, the next `get_delta` must include the var (pure python, mirror
`pure_delta_memo.py`). Must fail before the fix. Keep and, where useful, extend the existing #6946 dedupe tests.

E2E: copy `pure_delta_memo.py` to scratch, drop the `/envs/` assert, run with `.venv/bin/python` → steps 3/4 print
True (they print False with `envs/shared/bin/python` — record both). Then the browser repro: copy `event_loop/elapp`
to scratch, run it with your `.venv/bin/reflex` on your ports (dev), drive it with the driver venv and a copy of
`scripts/s_filtered.py` (read the script and NOTES.md for its arguments) → the page must show `secret-1` right after
`show`. Run the same against `envs/shared` to show the failure. Save stdout/frames under `fixes/f003/evidence/`.
