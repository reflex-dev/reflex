[
 {"goto": "/ref"},
 {"wait": 3500},
 {"shot": "tref_load"},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2), page: document.body.innerText.match(/Page \\d+ \\/ \\d+/)})", "note": "4 seeded rows visible"},
 {"click_nth": ["button:has-text('⋯')", 0], "note": "header dropdown"},
 {"wait": 800},
 {"click_text": "Clear all"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "clear_all empties the table AND the db"},
 {"click_text": "Seed"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "seed re-inserts 4"},
 {"shot": "tref_seeded"},
 {"click_text": "New Ticket"},
 {"wait": 1000},
 {"fill": ["input[placeholder='Title']", "QA manual ticket"]},
 {"fill": ["textarea[placeholder='Description']", "created through the UI by pre-release QA"]},
 {"fill": ["input[placeholder='Assignee']", "qa-ui"]},
 {"shot": "tref_form"},
 {"click_text": "Create"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2), first: (document.querySelector('tbody tr')||{}).innerText.replace(/\\n/g,' | ')})", "note": "new ticket lands at the top"},
 {"shot": "tref_created"},
 {"fill": ["input[placeholder='Search title, description, assignee']", "QA manual"]},
 {"click": "button[type=submit]"},
 {"wait": 2500},
 {"eval": "() => ({url: location.href.replace(location.origin,''), rows: document.querySelectorAll('tbody tr').length, first: (document.querySelector('tbody tr')||{}).innerText.replace(/\\n/g,' | ')})", "note": "search narrows to 1 and rewrites the URL"},
 {"shot": "tref_search"},
 {"reload": null},
 {"wait": 3500},
 {"eval": "() => ({url: location.href.replace(location.origin,''), rows: document.querySelectorAll('tbody tr').length, q: (document.querySelector(\"input[placeholder='Search title, description, assignee']\")||{}).value})", "note": "reload restores the filter from the URL"},
 {"click_text": "Reset filters"},
 {"wait": 2500},
 {"eval": "() => ({url: location.href.replace(location.origin,''), rows: document.querySelectorAll('tbody tr').length})", "note": "reset filters"},
 {"click_nth": ["thead button", 0], "note": "sort by title"},
 {"wait": 2000},
 {"eval": "() => ({url: location.search, order: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.split('\\n')[0])})"},
 {"click_nth": ["thead button", 0], "note": "toggle direction"},
 {"wait": 2000},
 {"eval": "() => ({url: location.search, order: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.split('\\n')[0])})"},
 {"shot": "tref_sorted"},
 {"click_nth": ["tbody tr button", 0], "note": "Start on the first row -> in_progress"},
 {"wait": 2000},
 {"eval": "() => Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.replace(/\\n/g,' | ')).slice(0,2)", "note": "status badge flips to in_progress"},
 {"click_nth": ["tbody tr button", 1], "note": "Close on the first row"},
 {"wait": 2000},
 {"eval": "() => ({rows: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.replace(/\\n/g,' | ')).slice(0,2), badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "closed; Open count drops"},
 {"shot": "tref_status"},
 {"click_nth": ["tbody tr button:has-text('⋯')", 0], "note": "row actions"},
 {"wait": 800},
 {"click_text": "Delete"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,2)})", "note": "row deleted"},
 {"shot": "tref_deleted"},
 {"goto": "/ticket-ref?ticket_id=0268223e-4c6d-4849-94a5-04d9eed6eb4f"},
 {"wait": 3000},
 {"eval": "() => ({inputs: Array.from(document.querySelectorAll('input')).map(e=>e.value), textarea: (document.querySelector('textarea')||{}).value, selects: Array.from(document.querySelectorAll('button[role=combobox]')).map(e=>e.innerText), body: document.body.innerText.replace(/\\n/g,' | ').slice(0,200)})", "note": "detail form pre-filled from the db"},
 {"shot": "tref_detail"},
 {"fill": ["input[placeholder='Title']", "Laptop won't boot (edited by QA)"]},
 {"fill": ["input[placeholder='Assignee']", "qa-editor"]},
 {"wait": 500},
 {"click_text": "Save changes"},
 {"wait": 2500},
 {"eval": "() => ({url: location.href.replace(location.origin,''), body: document.body.innerText.replace(/\\n/g,' | ').slice(0,200)})", "note": "save redirects back to the list"},
 {"shot": "tref_saved"},
 {"goto": "/ticket-ref?ticket_id=does-not-exist"},
 {"wait": 3000},
 {"eval": "() => document.body.innerText.replace(/\\n/g,' | ').slice(0,200)", "note": "unknown id -> callout"},
 {"shot": "tref_notfound"}
]
