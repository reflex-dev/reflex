[
 {"goto": "/"},
 {"wait": 3000},
 {"shot": "t_empty"},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText), buttons: Array.from(document.querySelectorAll('button')).map(e=>e.innerText).slice(0,14), inputs: Array.from(document.querySelectorAll('input')).map(e=>e.placeholder)})", "note": "empty DB after a fresh reflex db init"},
 {"click_text": "Seed"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,4), first_row: (document.querySelector('tbody tr')||{}).innerText, page: document.body.innerText.match(/Page \\d+ \\/ \\d+/)})", "note": "seed inserts demo rows"},
 {"shot": "t_seeded"},
 {"click_text": "New Ticket"},
 {"wait": 1200},
 {"fill": ["input[placeholder='Title']", "QA manual ticket"]},
 {"fill": ["textarea[placeholder='Description']", "created through the UI by pre-release QA"]},
 {"fill": ["input[placeholder='Assignee']", "qa-ui"]},
 {"wait": 600},
 {"shot": "t_newform"},
 {"click_text": "Create"},
 {"wait": 2500},
 {"eval": "() => ({rows: document.querySelectorAll('tbody tr').length, badges: Array.from(document.querySelectorAll('.rt-Badge')).map(e=>e.innerText).slice(0,4), rows_text: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.replace(/\\n/g,' | ')).slice(0,4)})", "note": "created ticket appears at the top (sort created_at desc)"},
 {"shot": "t_created"},
 {"fill": ["input[placeholder='Search title, description, assignee']", "QA manual"]},
 {"click_text": "Apply"},
 {"wait": 2500},
 {"eval": "() => ({url: location.search, rows: document.querySelectorAll('tbody tr').length, rows_text: Array.from(document.querySelectorAll('tbody tr')).map(r=>r.innerText.replace(/\\n/g,' | '))})", "note": "search filter + query params in the URL"},
 {"shot": "t_search"},
 {"reload": null},
 {"wait": 3000},
 {"eval": "() => ({url: location.search, rows: document.querySelectorAll('tbody tr').length, q: (document.querySelector(\"input[placeholder='Search title, description, assignee']\")||{}).value})", "note": "filters survive a reload through the URL"},
 {"click_text": "Reset filters"},
 {"wait": 2500},
 {"eval": "() => ({url: location.search, rows: document.querySelectorAll('tbody tr').length})", "note": "reset filters"},
 {"shot": "t_reset"},
 {"click_nth": ["thead button", 0], "note": "sort by title"},
 {"wait": 2000},
 {"eval": "() => ({url: location.search, first: (document.querySelector('tbody tr')||{}).innerText.split('\\n')[0], headers: Array.from(document.querySelectorAll('thead button')).map(e=>e.innerText.replace(/\\n/g,''))})"},
 {"click_nth": ["thead button", 0], "note": "toggle sort direction"},
 {"wait": 2000},
 {"eval": "() => ({url: location.search, first: (document.querySelector('tbody tr')||{}).innerText.split('\\n')[0]})"},
 {"shot": "t_sorted"}
]
