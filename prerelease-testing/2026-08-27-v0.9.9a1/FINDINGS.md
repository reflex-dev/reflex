# Findings — reflex 0.9.9a1 pre-release testing (2026-08-28)

Independent end-to-end exploration of the 2026-08-27 pre-release batch. All installs were
PyPI-only in isolated venvs (never from a checkout); every sample app was driven in real
Chromium via Playwright with server-log/console/network capture; every claimed issue was
independently re-reproduced by an adversarial verifier agent before being listed as
confirmed. 62 agents total across two workflow phases (feature exploration; upgrade +
enterprise regression), 0 agent errors.

## Executive summary

The release is in good shape for OSS users: all headline changelog items work as described
(routing fixes #6593/#6790/#6953/#6919, background-delta race #6920, memo auto-memoization
#6949, client_error #6827, logging pipeline #6863/#6865, pydantic-optional #6786,
RegistrationContext #6382, React Router 8 #6854/#6857, DevTools naming #6945), and the
0.9.8 -> 0.9.9a1 in-place upgrade produced ZERO functional regressions across ten
reflex-examples apps (including third-party reflex-global-hotkey / reflex-local-auth and
db/fastapi apps).

The blockers are downstream: published reflex-enterprise 0.9.4 breaks on 0.9.9a1 in two
independent ways (FINDING-001/021/022: bundled_libraries removal kills any ag-grid
python-callable renderer/formatter — masked by a misleading VarAttributeError — and
FINDING-023: DECORATED_PAGES removal breaks the flow demo at import). A coordinated
reflex-enterprise release or compat shims in reflex are needed before 0.9.9 final.
Also notable: the new PEP 695 alias support only half-works (FINDING-002/006: alias vars
compile but cannot be assigned at runtime), and background-task on_load is now cancelled
on navigation (FINDING-003, undocumented behavior change).

Severity histogram (29 confirmed findings): 1 critical, 4 high, 12 medium, 12 low.
Refuted claims (verified NOT 0.9.9a1 issues) are listed in their phase sections.

Index:
- FINDING-001: reflex-enterprise 0.9.4 incompatible with 0.9.9a1 — `dynamic.bundled_libraries` removed (HIGH)
- FINDING-002: Runtime assignment to PEP695-alias-annotated state var raises TypeError (State.__setattr__/_isinstance does not resolve TypeAliasType) — alias vars compile but cannot be mutated (HIGH)
- FINDING-003: Background-task on_load is cancelled on navigation (behavior change vs 0.9.8, not in changelog) (MEDIUM)
- FINDING-004: client_error socket handler raises unhandled TypeError on a no-argument emit, bypassing every anti-abuse guard (MEDIUM)
- FINDING-005: REFLEX_ENABLE_FULL_LOGGING: granian worker's file handler is closed — worker records silently dropped from the log file and legacy console file-writes leak to stdout (breaking --json purity) (MEDIUM)
- FINDING-006: Event handler with PEP695-alias-annotated arg passed UNCALLED to a trigger crashes page compile (typehint_issubclass does not resolve TypeAliasType) (MEDIUM)
- FINDING-007: Buffered/streamed upload sanitizer returns traversal token '..' for all-dots filenames, escaping the upload dir and causing an unhandled 500 (MEDIUM)
- FINDING-008: Breaking API removals ship without shims or pointer errors: get_config(reload=True) raises bare TypeError, bundled_libraries bare AttributeError (MEDIUM)
- FINDING-009: Undocumented behavior break: second bare rx.App() in one process now raises ReflexRuntimeError (0.9.8 allowed it); reflex.page.DECORATED_PAGES also removed undocumented with a confusing ImportError (MEDIUM)
- FINDING-010: library="react-router-dom" custom component: silent unpinned install + dev/prod divergence + cryptic prod failure (MEDIUM)
- FINDING-011: reflex run hangs indefinitely after fatal node-version error (REFLEX_USE_NPM path) (MEDIUM)
- FINDING-012: Prod build passes invalid/deprecated options to vite 8.2.0 (rolldown): 'jsx' input option rejected, 'advancedChunks' deprecated (LOW)
- FINDING-013: reflex run --json --loglevel debug (dev): granian's own startup/shutdown lines are plain text on stdout, breaking strict JSON-lines parsing (LOW)
- FINDING-014: rx.Model subclass with table=True gives bare TypeError instead of 'pip install reflex[db]' guidance when sqlmodel is absent (LOW)
- FINDING-015: Prod vite build logs 'Invalid input options: jsx key' and 'advancedChunks deprecated' warnings on every build (LOW)
- FINDING-016: reflex.testing (AppHarness) requires uvicorn and psutil that the published wheel does not declare (LOW)
- FINDING-017: compile_app() silently discards user/app-level bundle_library() registrations, and the dynamic serializer never rewrites subpath imports of bundled libs (LOW)
- FINDING-018: Failed REFLEX_USE_NPM run persists an inconsistent reflex.lock (npm<->bun switch breaks subsequent runs) (LOW)
- FINDING-019: vite 8.2.0 bump introduces two new warnings on every prod build/export ('jsx' invalid input option; advancedChunks deprecated) (LOW)
- FINDING-020: rx.plotly silently drops the id prop (react-plotly.js expects divId) (LOW)
- FINDING-021: reflex-enterprise 0.9.4 + reflex 0.9.9a1: any python-callable ag-grid renderer/formatter whose compiled return expr carries imports crashes app compile (FINDING-001 end-to-end); real AttributeError masked as 'VarAttributeError: Attribute _cached_get_all_var_data not found' (CRITICAL)
- FINDING-022: FINDING-001 (dnd/LambdaVar surface): non-static LambdaVar prop with bundle_library imports crashes with AttributeError at reflex_enterprise/vars.py:143 on 0.9.9a1 (HIGH)
- FINDING-023: 0.9.9a1 removes reflex.page.DECORATED_PAGES (PR #6382) with no shim and no changelog mention — breaks reflex-enterprise flow demo at import with a confusing error (HIGH)
- FINDING-024: reflex-base CachedVarOperation masks any AttributeError raised inside cached var computations, producing a misleading unchained 'Attribute _cached_get_all_var_data not found' (MEDIUM)
- FINDING-025: shipped rxe ag_grid demo is broken against reflex >= 0.9.8 regardless of 0.9.9a1: bundle_library('$/utils/components') is a stale pre-0.9.8 memo path (MEDIUM)
- FINDING-026: reflex-enterprise HTTPCookie.sync(): /_reflex/cookies/sync endpoint 404s in the OIDC demo (route registration never reaches the serving backend worker) — broken on 0.9.8 too (MEDIUM)
- FINDING-027: 0.9.9a1 console warnings print literal backslash-escaped brackets (dict\[str, ...]) (LOW)
- FINDING-028: New vite warning on every dev start: template vite.config.js imports vite-plugin-safari-cachebust without file extension (unsupported by future configLoader:'native' default) (LOW)
- FINDING-029: reflex-enterprise 0.9.4 error paths emit console.error DeprecationWarning on 0.9.9a1 (app.py:120 login gate, utils.py:119 prod gate) (LOW)

## Versions under test (all published on PyPI ✓, verified via /pypi/<pkg>/<ver>/json)

reflex 0.9.9a1, reflex-base 0.9.9a1, reflex-components-core 0.9.9a1,
reflex-components-radix 0.9.8a1, reflex-components-code 0.9.4a1,
reflex-components-plotly 0.9.5a1, reflex-components-sonner 0.9.2a1,
reflex-components-dataeditor 0.9.2a1, reflex-components-lucide 1.0.4a1,
reflex-components-react-player 0.9.2a1, reflex-docgen 0.9.5a1, reflex-hosting-cli 0.1.71a2.
(markdown 0.9.3 / moment 0.9.3 / recharts 0.9.2 / reflex-release 0.1.0 unchanged, not alpha.)

Environment: Linux container, 4 CPU / 15GB, Node v22.22.2 (floor is 22.22.0), bun 1.3.11,
Python 3.11.15 primary (3.12/3.13/3.14 via uv), Chromium via Playwright, outbound via agent proxy.

## FINDING-001: reflex-enterprise 0.9.4 incompatible with 0.9.9a1 — `dynamic.bundled_libraries` removed (HIGH)

- Breaking change #6382 moved the module-level `bundled_libraries` list from
  `reflex_base.components.dynamic` onto the active `RegistrationContext`. No shim:
  `reflex.components.dynamic.bundled_libraries` now raises `AttributeError` (verified in
  isolated venv with published packages, see repro below).
- Latest published reflex-enterprise (0.9.4) wheel reads it at `reflex_enterprise/vars.py:143`
  (`bundled_libraries = set(dynamic.bundled_libraries)`) inside
  `LiteralLambdaVar._validate_and_extend_return_expr`, used by the LambdaVar machinery that
  dnd, map, and ag_grid components rely on for lambda props whose compiled JS carries imports.
- Expected user impact: `AttributeError: module 'reflex.components.dynamic' has no attribute
  'bundled_libraries'` at compile time for affected enterprise apps on 0.9.9a1.
- Repro (no app needed):
  ```
  uv venv /tmp/v && uv pip install --python /tmp/v/bin/python --prerelease=allow 'reflex==0.9.9a1'
  /tmp/v/bin/python -c "from reflex.components import dynamic; dynamic.bundled_libraries"
  # AttributeError
  ```
  End-to-end demo repro: see enterprise workflow agent results.
- Note: changelog for reflex-base 0.9.9a1 documents the move ("use bundle_library() /
  reset_bundled_libraries() as before") but downstream reflex-enterprise has not been updated;
  either a compat shim (module `__getattr__` reading the active RegistrationContext) or a
  coordinated reflex-enterprise release is needed before 0.9.9 final.

## FINDING-002: Runtime assignment to PEP695-alias-annotated state var raises TypeError (State.__setattr__/_isinstance does not resolve TypeAliasType) — alias vars compile but cannot be mutated (HIGH)

- Cluster: `typing_python` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: uv venv v --python 3.12 && uv pip install --python v/bin/python --prerelease=allow 'reflex==0.9.9a1'; then run prerelease-testing/2026-08-27-v0.9.9a1/typing_python/repro_alias_setattr.py — every setattr (plain alias, Literal alias, Items[str], Key|None) crashes; also repro_alias_backport_311.py on 3.11 (typing_extensions backport). End-to-end: run pep695app (reflex run --frontend-port 3340 --backend-port 8340), click any button — state never updates, backend logs the TypeError.
- Evidence: reflex/state.py:1544 __setattr__ -> reflex_base/utils/types.py:872 _isinstance -> 'TypeError: isinstance() arg 2 must be a type, a tuple of types, or a union'. The _isinstance guard is only meant to logger.error a mismatch but raises instead. Full tracebacks in typing_python/logs/pep695_312_server.log; screenshot shots/pep695_312/02_after_clicks_no_update.png. Not a regression only because 0.9.8 crashed one stage earlier (guess_type TypeError at class definition), but it renders #6944's advertised feature unusable for any var that is ever reassigned.
- Root cause (verifier analysis): PR #6944 added resolve_type_alias() to reflex_base/utils/types.py but wired it only into Var.guess_type (sole call site: reflex_base/vars/base.py:1080). The runtime validation guard in BaseState.__setattr__ (reflex/state.py:1544) passes the UNRESOLVED field.outer_type_ (a TypeAliasType) to _isinstance(), which falls through to the bare `return isinstance(obj, cls)` at reflex_base/utils/types.py:872; isinstance() rejects TypeAliasType as arg 2 and raises. The guard was designed to only logger.error on mismatch, but the TypeError escapes and aborts event processing. Fix direction: resolve TypeAliasType (recursively, including subscripted aliases and aliases inside unions) at the top of _isinstance/typehint_issubclass — the latter also fixes the sibling BUG 1 (event-handler arg spec comparison) — or resolve field.outer_type_ once at model-field construction time.
- Verification notes: CONFIRMED as a genuine 0.9.9a1 defect (high severity is fair). Independent reproduction in a FRESH 3.12 venv (PyPI-only: reflex==0.9.9a1 + reflex-base 0.9.9a1): (1) repro_alias_setattr.py — all four alias-annotated assignments (plain alias, Literal alias, Items[str], Key|None) crash with "TypeError: isinstance() arg 2 must be a type, a tuple of types, or a union"; in-place dict mutation passes. (2) End-to-end with my OWN minimal app (not the claimant's; /home/user/reflex/prerelease-testing/2026-08-27-v0.9.9a1/typing_python/verification/aliasapp, ports 3840/8840, Playwright/Chromium): a control button assigning a plain str var updates the UI fine, while the button assigning k: Key = "b" (a VA

## FINDING-003: Background-task on_load is cancelled on navigation (behavior change vs 0.9.8, not in changelog) (MEDIUM)

- Cluster: `routing` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: App with @rx.page(route='/slowbg', on_load=SlowBgState.slow_bg_load) where slow_bg_load is @rx.event(background=True) doing `async with self` writes 1s apart. Load /slowbg, wait for the first write, client-nav to another page, wait 5s: on 0.9.9a1 no further writes ever land (task cancelled silently, no log traceback); on 0.9.8 the task completes all writes. Button-started background tasks still survive navigation on 0.9.9a1, so the cancellation is specifically the on_load chain superseding (#6593) reaching its background child. Run: prerelease-testing/2026-08-27-v0.9.9a1/routing/drive_routing.py <base> shots bg against routing_app (0.9.9a1) and routing098 (0.9.8).
- Evidence: 0.9.9a1: bg_progress='39.0|bg1-start' frozen 5.5s after nav (dev and prod runs, shots/07_bg_after_nav.png, shots_prod/07_bg_after_nav.png). 0.9.8: bg_progress='37.4|bg1-start ; 38.4|bg1-step1 ; ... ; 41.4|bg1-step4' (survived; shots098/07_bg_after_nav.png). Contrast: button-started bg task on 0.9.9a1 completes after nav (shots_prod/14_btnbg_after_nav.png). If intended, the #6593 changelog entry should call out that background on_load tasks are now cancelled on navigation.
- Root cause (verifier analysis): PR #6713 (issue #6593) marks on_load_internal supersedes=True; EventProcessor._supersede_previous cancels the previous unfinished chain on the next navigation and _on_future_done cascades cancel() to ALL child futures and their asyncio tasks (packages/reflex-base/src/reflex_base/event/processor/event_processor.py). A background on_load handler is a child future of the on_load chain and keeps the chain not-all_done() for its entire runtime, so any subsequent navigation cancels the running background task mid-flight. The cascade does not exempt is_background children, and nothing in tests/changelog indicates this was intended.
- Verification notes: Reproduced independently with a fresh minimal app (3 pages) written from the repro steps alone, PyPI installs only. On 0.9.9a1 (smoke venv, dev server ports 3600/8600): a @rx.event(background=True) on_load on /slowbg writes its start entry, then client-nav to /other -> zero further writes 6s later; hard reload of /other still shows only the start entry, proving the `async with self` writes never happened server-side (real task cancellation, not missing deltas). Server log shows only a Debug 'Cancelling the previous unfinished ...on_load_internal chain' line — no traceback. Controls: staying on the page completes all steps on 0.9.9a1, and an identical button-started background task survives n

## FINDING-004: client_error socket handler raises unhandled TypeError on a no-argument emit, bypassing every anti-abuse guard (MEDIUM)

- Cluster: `client_error` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: On reflex 0.9.9a1: (1) build a blank app, run backend: `REFLEX_TELEMETRY_ENABLED=false NO_PROXY=localhost,127.0.0.1 reflex run --backend-only --backend-port 8220 --loglevel info > be.log 2>&1 &` ; wait for :8220/ping==200. (2) With python-socketio, connect WITHOUT a token (unknown sid) and emit the event with no data: `c=socketio.AsyncClient(); await c.connect('http://localhost:8220', socketio_path='_event', namespaces=['/_event'], transports=['websocket']); await c.emit('client_error', namespace='/_event')` (repeat N times). (3) `grep -c 'missing 1 required positional argument' be.log` returns 2*N; `grep -c 'unknown SID' be.log` returns 0. Root cause: reflex/app.py EventNamespace.on_client_error(self, sid, data) has no default for `data`; python-socketio invokes handlers as handler(sid, *data[1:]), so a no-arg emit calls on_client_error(sid) and raises 'TypeError: on_client_error() missing 1 required positional argument: data' inside socketio's _handle_event_internal, BEFORE the isinstance(data,dict) guard, the `if sid not in self.sid_to_token` unknown-sid gate, and the per-SID (_MAX_CLIENT_ERRORS_PER_SID=5) / per-window (_MAX_CLIENT_ERRORS_PER_WINDOW=20) rate limiters. It surfaces as an asyncio 'Task exception was never retrieved' traceback (not via the reflex logger) so it is visible even at default info loglevel and unsuppressable via --loglevel. Impact: any connected socket, with no valid token, can flood backend logs with tracebacks — an unauthenticated log-spam DoS that defeats the very hardening #6827 added. Not a crash (server continues). Scripts: artifacts noarg_unlinked.py, abuse2.py; evidence artifacts/evidence_noarg_typeerror.txt. Suggested fix (not applied): default data (data: Any = None) so the existing isinstance guard handles it. Note this is a new handler introduced in 0.9.9a1, so there is no 0.9.8 behavior to regress from.
- Root cause (verifier analysis): reflex/app.py:2169 `async def on_client_error(self, sid: str, data: Any)` gives `data` no default. python-socketio (5.16.4) dispatches events as `_trigger_event(data[0], namespace, sid, *data[1:])`, so an `emit("client_error")` with no payload calls the handler as `on_client_error(sid)`, raising `TypeError: missing 1 required positional argument: 'data'` at socketio async_server.py:621 — before the handler body runs. All four #6827 guards (isinstance-dict @2189, unknown-sid gate @2196, per-SID cap @2204, per-window cap @2213) live after the parameter binding, so the no-arg path bypasses every one. The exception surfaces via asyncio's default handler ("Task exception was never retrieved"), so it prints at default info loglevel and is unsuppressable via --loglevel. Fix: default `data: Any = None`; the existing `isinstance(data, dict)` guard then handles the None/missing case at debug with no traceback.
- Verification notes: CONFIRMED — genuine framework defect a fix-agent should act on. Severity medium is appropriate (not a crash; server keeps serving).

Independent reproduction (from repro steps alone, not the claimant's app):
- Fresh `reflex init --template blank` app in /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_client_error_0/, reflex==0.9.9a1 (smoke venv, PyPI), backend-only on reserved port 8720 at --loglevel info. Confirmed :8720/ping==200.
- Driver: /tmp/.../scratchpad/apps/verify_client_error_0/verify_noarg.py run with $SB/envs/client_error_drv/bin/python (python-socketio 5.16.4, aiohttp 3.14.3). NOTE: the smoke venv's own socketio client lacks aiohttp, 

## FINDING-005: REFLEX_ENABLE_FULL_LOGGING: granian worker's file handler is closed — worker records silently dropped from the log file and legacy console file-writes leak to stdout (breaking --json purity) (MEDIUM)

- Cluster: `logging_cli` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: cd prerelease-testing/2026-08-27-v0.9.9a1/logging_cli/logapp; REFLEX_TELEMETRY_ENABLED=false REFLEX_ENABLE_FULL_LOGGING=1 REFLEX_LOG_FILE=$PWD/full.log <venv>/bin/reflex run --loglevel info --frontend-port 3261 --backend-port 8261; open the app and click #btn-inc (event handler calls console.info/warn and a reflex_base.* logger). Observe: (1) full.log contains only main-CLI-process records — the worker's records are absent (grep 'hierarchy' full.log = 0); (2) stdout shows duplicated '[YYYY-mm-dd HH:MM:SS.ffffff] Info: ...' timestamped lines (the intended file copies); (3) with --json added, those lines appear as raw non-JSON plain text interleaved with JSON records. Diagnosis: click #btn-diag2 (drive_diag.py) — in the worker, reflex_base.utils.log._file_handler().stream is None (handler closed, baseFilename correct, handler still attached). Python 3.11 FileHandler will not reopen a closed mode='w' handler => records drop; console.log_file_stream() returns the None stream => rich Console(file=None) => stdout. Probable closer: granian's post-fork logging configuration closing pre-existing handlers. Single-process use (plain python with REFLEX_MANAGED_LOGGING=true) works correctly.
- Evidence: logging_cli/logs/run_plain.stdout (duplicated timestamped lines), logging_cli/logs/full_log.log (no worker records), logging_cli/logs/run_json_full.stdout (19 non-JSON lines among JSON records — check_jsonlines.py output), drive_diag.py DIAG2 output: 'type=NoneType ... base=<REFLEX_LOG_FILE path> handlers=[RichConsoleHandler, FileHandler]'
- Root cause (verifier analysis): granian worker post-fork logging setup (granian/server/mp.py wrap_target -> granian.log.configure_logging -> logging.config.dictConfig) runs stdlib _clearExistingHandlers(), which closes every handler in logging._handlerList including the fork-inherited reflex FileHandler. FileHandler.close() sets stream=None/_closed=True; py3.11+ FileHandler.emit will not reopen a closed mode='w' handler, so all worker records are silently dropped from the full-logging file; reflex_base.utils.log.log_file_stream() then returns the None stream (despite its comment claiming it never can) and console.print_to_log_file's rich Console(file=None) falls back to stdout, leaking timestamped plain-text lines that break --json purity. Regression introduced by PR #6863's rewrite: 0.9.8's log_file_console opened its own plain append-mode file object that dictConfig could not touch.
- Verification notes: CONFIRMED as a genuine 0.9.9a1 regression, reproduced independently from the repro steps alone (fresh app copy in /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_logging_cli_0/, ports 3760-3762/8760-8762, real reflex run + Chromium/Playwright clicks on #btn-inc). Evidence: (1) 0.9.9a1 plain mode: full.log has 0 worker records ('event handler'/'hierarchy' grep = 0) while stdout shows each worker console.info/warn line twice (normal render + leaked '[YYYY-mm-dd HH:MM:SS.ffffff] ...' file-copy); (2) 0.9.9a1 --json mode: 11 non-JSON plain-text lines interleaved with 10 valid JSON records on stdout (leaks even wrap across multiple lines), full_json.log 

## FINDING-006: Event handler with PEP695-alias-annotated arg passed UNCALLED to a trigger crashes page compile (typehint_issubclass does not resolve TypeAliasType) (MEDIUM)

- Cluster: `typing_python` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Same venv; run prerelease-testing/2026-08-27-v0.9.9a1/typing_python/repro_alias_event_arg.py — rx.input(on_change=S.choose) with 'def choose(self, value: Key)' where 'type Key = Literal[...]' raises fatal TypeError 'Could not compare types <class str> and Key'; a parameterized alias arg (form_data: Pair[str, str]) instead raises fatal EventHandlerArgTypeMismatchError claiming got Pair[str, str] where dict expected. Pre-called (S.choose('a')) and lambda-wrapped bindings work.
- Evidence: reflex_base/event/__init__.py _check_event_args_subclass_of_callback -> reflex_base/utils/types.py:1192 typehint_issubclass -> 'issubclass() arg 2 must be a class, a tuple of classes, or a union'. First traceback preserved in logs/pep695_312_server.log (initial run before workaround). 0.9.8 fails identically on an event-handler-only app, so not a regression — but #6944 resolves aliases only in Var.guess_type, so users combining the newly-supported alias state vars with the same alias on handler args hit a hard compile crash.
- Root cause (verifier analysis): PR #6944 added resolve_type_alias() to reflex_base/utils/types.py but wired it only into Var.guess_type (reflex_base/vars/base.py:1080). typehint_issubclass() never resolves TypeAliasType: for a bare alias both get_origin() results are None so it falls through to bare issubclass(str, Key) at types.py:1192 (TypeError, re-raised fatally by _check_event_args_subclass_of_callback at event/__init__.py:1999); for a subscripted alias like Pair[str, str] the origin is the TypeAliasType itself, so no comparison branch matches and it returns False, turning what would be at most a warning for the resolved dict[str, str] into a fatal EventHandlerArgTypeMismatchError. Fix direction: resolve type aliases (both operands) at the top of typehint_issubclass (and likely _isinstance, which is BUG 2's twin of the same omission).
- Verification notes: CONFIRMED as a genuine 0.9.9a1 framework defect (medium severity fair; explicitly NOT a regression vs 0.9.8). Reproduced independently from the repro steps alone in fresh PyPI-only venvs (Python 3.12.3, reflex==0.9.9a1/reflex-base 0.9.9a1; separate fresh venv with reflex==0.9.8 for baseline), with my own scripts written from the claim: /home/user/reflex/prerelease-testing/2026-08-27-v0.9.9a1/typing_python/verification/verify_bug1_099a1.py and verify_bug1_098_baseline.py. Results: (1) rx.input(on_change=S.choose) with 'def choose(self, value: Key)' where 'type Key = Literal["a","b"]' raises fatal TypeError 'Could not compare types <class str> and Key for argument value of S.choose for on_chan

## FINDING-007: Buffered/streamed upload sanitizer returns traversal token '..' for all-dots filenames, escaping the upload dir and causing an unhandled 500 (MEDIUM)

- Cluster: `components` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Start comp_app (reflex run --frontend-port 3380 --backend-port 8380). Run: $SB/envs/driver/bin/python $SB/apps/components/drive_upload_dotdot.py http://localhost:3380 http://localhost:8380 -> filenames '..','./../.','..\\','/..' return status 500; '...' returns 200. Or offline: $SB/envs/smoke/bin/python -c "from reflex_components_core.core._upload import _sanitize_upload_filename as s; print(repr(s('..')))" -> '..'
- Evidence: reflex-components-core/core/_upload.py::_sanitize_upload_filename: for '..','./../.','..\\','/..' safe_parts is empty and it returns windows_path.name, and PureWindowsPath('..').name=='..'. Live buffered endpoint (drive_upload_dotdot.py): POST filename '..' -> file.path=Path('..') -> get_upload_dir()/file.path == uploaded_files/.. (parent of upload dir) -> handler write_bytes raises IsADirectoryError [Errno 21] Is a directory: 'uploaded_files/..' -> HTTP 500. Server-log traceback saved to artifacts/dotdot_traceback.txt. '...' (non-traversal) correctly -> '...' contained, 200. Offline: $SB/envs
- Root cause (verifier analysis): reflex-components-core 0.9.9a1 core/_upload.py::_sanitize_upload_filename (introduced by #6753). For a filename composed entirely of traversal/empty segments ('..', './../.', '..\\', '/..'), the safe_parts list is empty, so the function falls through to `return windows_path.name`, and PureWindowsPath('..').name == '..'. It thus returns the bare traversal token '..' despite its docstring promising "A safe relative upload path". Handlers (including the canonical `get_upload_dir() / file.name`, since UploadFile.name == self.path.name == Path('..').name == '..') then build `uploaded_files/..` (the upload dir's parent), and write_bytes on that directory raises an unhandled IsADirectoryError inside the ndjson streaming generator -> HTTP 500. Fix must return a generated safe name or reject with 4xx when no real segment survives; returning '' is insufficient because Path('') == Path('.') still points at the upload dir and 500s the same way.
- Verification notes: CONFIRMED as a genuine (narrow, low/medium-severity) framework defect a fix-agent should act on. Reproduced independently both offline and live.

OFFLINE (smoke venv, reflex-components-core 0.9.9a1 confirmed): _sanitize_upload_filename('..')=='..', ('./../.')=='..', ('..\\')=='..', ('/..')=='..'; '...'->'...' , '../x.txt'->'x.txt' (traversal segment stripped), 'a/../b'->'a/b'. Exactly matches the claim's offline evidence.

LIVE (comp_app on my reserved ports 3880/8880, real Playwright token, httpx raw multipart to /_upload): filenames '..','./../.','..\\','/..' each return HTTP 500 body 'Internal server error'; '...' -> 200 contained (path='...'); '../x.txt' -> 200 with path stripped to 'x.t

## FINDING-008: Breaking API removals ship without shims or pointer errors: get_config(reload=True) raises bare TypeError, bundled_libraries bare AttributeError (MEDIUM)

- Cluster: `registration_context` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: uv venv /tmp/v && uv pip install --python /tmp/v/bin/python --prerelease=allow 'reflex==0.9.9a1'; /tmp/v/bin/python -c "import reflex as rx; rx.config.get_config(reload=True)" -> TypeError: get_config() got an unexpected keyword argument 'reload'; /tmp/v/bin/python -c "import reflex.components.dynamic as d; d.bundled_libraries" -> AttributeError. Both worked on 0.9.8. Full matrix: test_api_surface.py in artifacts dir.
- Evidence: api_surface_099a1.txt vs api_surface_098.txt in /home/user/reflex/prerelease-testing/2026-08-27-v0.9.9a1/registration_context/. Errors never mention reload_config() or the RegistrationContext move; repo's own CLAUDE.md deprecation policy asks for a fallback path. bundled_libraries half is FINDING-001 (breaks reflex-enterprise 0.9.4 LiteralLambdaVar); a module __getattr__ shim + console.deprecate would cover both.
- Root cause (verifier analysis): PR #6382 moved get_config's reload behavior to a new reload_config() and moved the module-level bundled_libraries list onto the active RegistrationContext, deleting the old public names outright without a compat shim (no deprecated reload kwarg, no module __getattr__ emitting console.deprecate), so 0.9.8-era callers — including reflex-enterprise 0.9.4's LiteralLambdaVar — hit bare TypeError/AttributeError with no pointer to the replacement APIs.
- Verification notes: CONFIRMED as a genuine 0.9.9a1 regression, independently reproduced from the repro steps alone in fresh PyPI-only venvs (uv venv py3.11: reflex==0.9.9a1 and reflex==0.9.8 baseline). (1) rx.config.get_config(reload=True) raises bare "TypeError: get_config() got an unexpected keyword argument 'reload'" on 0.9.9a1; on 0.9.8 the public signature is get_config(reload: bool = False) and the call returns Config. The replacement reload_config() exists in 0.9.9a1 but the error never mentions it; the wheel's reflex_base/config.py has no shim even though console.deprecate is used elsewhere in the same file. (2) reflex.components.dynamic.bundled_libraries raises bare AttributeError on 0.9.9a1; on 0.9.8 

## FINDING-009: Undocumented behavior break: second bare rx.App() in one process now raises ReflexRuntimeError (0.9.8 allowed it); reflex.page.DECORATED_PAGES also removed undocumented with a confusing ImportError (MEDIUM)

- Cluster: `registration_context` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: python -c "import reflex as rx; rx.App(); rx.App()" -> ReflexRuntimeError 'A RegistrationContext can only be associated with a single App instance...' on 0.9.9a1, OK on 0.9.8. python -c "from reflex.page import DECORATED_PAGES" -> ImportError: cannot import name 'DECORATED_PAGES' from 'PageNamespace' (unknown location) on 0.9.9a1; was a defaultdict on 0.9.8.
- Evidence: packages/reflex-base/news/6382.breaking.md lists only get_config and bundled_libraries; the 6382.feature.md fragment describes fork() but not the raise. The App() message itself is good (names fork()), but notebook cell re-runs / doc generators / multi-app scripts break silently-undocumented; the DECORATED_PAGES ImportError text is confusing because sys.modules['reflex.page'] is a PageNamespace instance.
- Root cause (verifier analysis): PR #6382 intentionally moved App/Config/decorated-pages/bundled-libraries from module globals onto RegistrationContext: reflex_base/registry.py _set_app now raises ReflexRuntimeError when the context already holds an App (one-App-per-context invariant, fork() escape hatch), and reflex/page.py deleted the DECORATED_PAGES module global plus its deliberate PageNamespace class re-export without a __getattr__/deprecation shim — the confusing ImportError text comes from the pre-existing sys.modules['reflex.page'] = PageNamespace replacement. The release process then under-documented it: the reflex-base 6382.breaking.md fragment covers only get_config/bundled_libraries and the metapackage changelog files the scoping change under Features, so both user-visible breaks shipped unlisted as breaking changes.
- Verification notes: Reproduced both claims independently in fresh PyPI venvs (scratchpad envs/vregctx_a1 reflex==0.9.9a1, envs/vregctx_098 reflex==0.9.8) via /home/user/reflex/prerelease-testing/2026-08-27-v0.9.9a1/registration_context/verify_repro.py (output: verify_output.txt in same dir). 0.9.9a1: second bare rx.App() raises ReflexRuntimeError from reflex_base/registry.py _set_app; from reflex.page import DECORATED_PAGES raises ImportError "cannot import name 'DECORATED_PAGES' from 'PageNamespace' (unknown location)". 0.9.8: both work (two distinct App instances; defaultdict). Refutation attempts failed: not an env quirk (caught and corrected one — running python -c with cwd=/home/user/reflex shadows the ven

## FINDING-010: library="react-router-dom" custom component: silent unpinned install + dev/prod divergence + cryptic prod failure (MEDIUM)

- Cluster: `prod_export` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: In a 0.9.9a1 app add: class BadDomLink(rx.Component): library='react-router-dom'; tag='Link'; to: rx.Var[str]; render it on a page. 1) 'reflex run' (dev): reflex runs bun add with bare 'react-router-dom' (resolves to ^7.18.2, unpinned) next to react-router 8.3.0; component renders and navigates with no warning; reflex.lock is updated with the stray dep. 2) 'reflex run --env prod --frontend-port 8464 --backend-port 8464': build fails at prerender, exit 1.
- Evidence: logs/prod_baddom.log: 'TypeError: Cannot destructure property basename of React10.useContext(...) as it is null' at LinkWithRef (.web/node_modules/react-router-dom/node_modules/react-router/dist/development/chunk-62JRHF6Z.mjs) -> 'Error: Prerender: Request failed for /: ... 500 ... entry.server.tsx'. dev_run.log lines ~215+: 'bun add ... react-router-dom ...' -> 'installed react-router-dom@7.18.2'. On 0.9.8 react-router-dom was a legitimate wrapped library so such components worked in dev AND prod; a targeted error/warning ('react-router-dom is not used with React Router 8 - use library="react
- Root cause (verifier analysis): React Router 8 dropped the react-router-dom re-export package, and 0.9.9a1 accordingly removed it from PackageJson.DEPENDENCIES (per #6854). A custom component declaring library="react-router-dom" therefore falls through App._get_frontend_packages' generic custom-library path: `bun add react-router-dom` (unpinned, resolves to v7.18.2 latest), which nests a second react-router@7 copy under it beside the first-party react-router@8.3.0. The v7 Link reads v7's NavigationContext — a different context object from the v8 router's — which is null during SSR/prerender, producing the cryptic basename TypeError and build exit 1; in dev, vite's module resolution dedupes to the hoisted v8 copy so the component works silently. Missing piece: a targeted compile/install-time warning or error for library="react-router-dom" (suggesting library="react-router" / "react-router/dom"), which the framework could cheaply emit since it knows the name from its own migration.
- Verification notes: Independently reproduced from repro steps alone with a fresh minimal app (BadDomLink: library="react-router-dom", tag="Link") on reflex 0.9.9a1 (shared smoke venv), plus a 0.9.8 PyPI baseline in a separate venv. All four claimed behaviors confirmed: (1) dev (ports 3960/8960): reflex runs `bun add` with bare unpinned 'react-router-dom' among otherwise exactly-pinned deps -> installed react-router-dom@7.18.2 (^7.18.2 in .web/package.json) next to react-router 8.3.0, with nested react-router@7.18.2 under it; stray dep persisted into reflex.lock/package.json; Playwright: link renders and navigates, zero console errors/warnings, no reflex warning. (2) prod (--env prod, single port 8960): exit 1 a

## FINDING-011: reflex run hangs indefinitely after fatal node-version error (REFLEX_USE_NPM path) (MEDIUM)

- Cluster: `prod_export` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Put a node shim printing v22.12.0 first on PATH (see fakenode/node), then: REFLEX_USE_NPM=1 timeout 150 reflex run --frontend-port 3463 --backend-port 8465. The correct error prints ('Reflex requires node version 22.22.0 or higher...'), granian logs 'Unexpected exit from worker-1' - and then the parent process never exits (killed by timeout; exit=124 in both 90s and 150s runs; 'Reflex app stopped.' only appears via the atexit handler on SIGTERM).
- Evidence: logs/oldnode_npm.log and logs/oldnode_npm2.log (both end at the error yet the process ran until timeout). The SystemExit(1) raised in js_runtimes.validate_frontend_dependencies inside the granian worker does not terminate the supervising process. 22.22.0 floor is new in 0.9.9a1 so no direct 0.9.8 comparison, but a fatal preflight failure should exit non-zero promptly.
- Root cause (verifier analysis): SystemExit(1) from js_runtimes.validate_frontend_dependencies (reflex/utils/js_runtimes.py:377) is raised inside the run_frontend ThreadPoolExecutor thread created by processes.run_concurrently_context, whose futures are only checked after the with-body — but the main thread is blocked forever in granian serve() (exec.run_backend), so the exception is swallowed and the process never exits; the backend keeps serving with no frontend.
- Verification notes: REPRODUCED twice from repro steps alone (fresh blank app, fake node v22.12.0 shim, REFLEX_USE_NPM=1): fatal node-floor error prints in seconds, `reflex run` then runs until killed (exit=124 at 90s and 240s). MECHANISM CORRECTION vs the report: the SystemExit is NOT in a granian worker and '[ERROR] Unexpected exit from worker-1' is only SIGTERM-teardown noise (absent during the whole hang in an instrumented run; the granian backend worker stays alive and GET /ping returns 200 while the frontend port never listens). Root cause: reflex/reflex.py::_run_dev submits exec.run_frontend to a ThreadPoolExecutor via processes.run_concurrently_context (reflex/utils/processes.py:266-302) and blocks the m

## FINDING-012: Prod build passes invalid/deprecated options to vite 8.2.0 (rolldown): 'jsx' input option rejected, 'advancedChunks' deprecated (LOW)

- Cluster: `memo` | Regression vs 0.9.8: n/a | Verifier: CONFIRMED
- Repro: cd memoapp (any 0.9.9a1 app) && reflex run --env prod --frontend-port 8141 --backend-port 8141 --loglevel debug; observe build warnings before 'transforming...'
- Evidence: prod_server.log (artifact dir) ~lines 142-146: 'Warning: Invalid input options (1 issue found) - For the "jsx". Invalid key: Expected never but received "jsx".' and 'WARN advancedChunks option is deprecated, please use codeSplitting instead.' Build succeeds; cosmetic today but will break when rolldown removes the deprecated option.
- Root cause (verifier analysis): reflex's hardcoded vite config template (packages/reflex-base/src/reflex_base/compiler/templates.py, vite.config.js generator) passes rolldown-era options that vite 8.x's stricter rolldown validation now flags: `rollupOptions.jsx: {}` (key rejected — JSX is handled by vite/oxc, not a rollupOptions input option) and `rollupOptions.output.advancedChunks` (renamed to `codeSplitting` in newer rolldown). Present unchanged since at least 0.9.8 (vite 8.0.16); the vite bump to 8.2.0 in 0.9.9a1 did not introduce it.
- Verification notes: CONFIRMED as a genuine framework defect, with one important correction: it is NOT a 0.9.9a1 regression — stable 0.9.8 has the identical behavior. Independent repro on a fresh `reflex init --template blank` app (reflex 0.9.9a1 from PyPI, no memo-cluster features): `reflex run --env prod --frontend-port 8640 --backend-port 8640 --loglevel debug` → vite v8.2.0 emits `Warning: Invalid input options (1 issue found) - For the "jsx". Invalid key: Expected never but received "jsx"` and `WARN advancedChunks option is deprecated, please use codeSplitting instead`, twice (client + ssr environment builds). Build succeeds and the app serves HTTP 200, so severity=low/cosmetic is accurate. Attribution veri

## FINDING-013: reflex run --json --loglevel debug (dev): granian's own startup/shutdown lines are plain text on stdout, breaking strict JSON-lines parsing (LOW)

- Cluster: `logging_cli` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: cd logapp; reflex run --json --loglevel debug --frontend-port 3260 --backend-port 8260 > out 2>err; wait for startup; kill. python check_jsonlines.py out => 9 non-JSON lines: '[INFO] Starting granian (main PID: ...)', '[INFO] Listening at: ...', '[INFO] Spawning worker-1...', '[INFO] Started worker-1', shutdown lines. Not present at info/default loglevel or in --env prod. If --json guarantees parseable stdout, granian needs a JSON log config (granian supports log_dictconfig).
- Evidence: logging_cli/logs/run_json.stdout lines 85-121 (check_jsonlines.py listing in NOTES.md)
- Root cause (verifier analysis): reflex/utils/exec.py::run_granian_backend (and run_granian_backend_prod, line 811) constructs granian.server.Server with log_level=LogLevels(loglevel.value) but never passes log_dictconfig/log_enabled, so granian's `_granian` logger keeps its default plain-text stdout handler ("[%(levelname)s] %(message)s", propagate=False) even when REFLEX_LOG_JSON is on; the new logging pipeline (reflex_base/utils/log.py) never configures `_granian` (granian is referenced there only for frame-info exclusion). It only surfaces at --loglevel debug because granian's level is config.loglevel.subprocess_level() (DEFAULT->WARNING) and the --loglevel flag reaches config only via the REFLEX_LOGLEVEL env var, which set_log_level (log.py:591) skips setting when the requested level equals the current one — the initial level is INFO, so `--loglevel info` is a no-op for config.loglevel (side quirk) while `--loglevel debug` propagates and granian's INFO startup/shutdown lines print raw. Fix direction: pass a JSON-formatting log_dictconfig (or log_enabled=False + re-emit) to granian when is_json_mode(), in both dev and prod granian paths.
- Verification notes: REPRODUCED exactly as claimed. Fresh working dir ($SB/apps/verify_logging_cli_1/logapp, copy of the reporter's app), shared PyPI smoke venv (reflex 0.9.9a1, granian 2.8.2), ports 3764/8764. `reflex run --json --loglevel debug` (dev): stdout = 106 lines, 98 valid JSON, 8 plain-text granian lines ([INFO] Starting granian (main PID)/Listening at/Spawning worker-1/Started worker-1 + 4 shutdown lines incl. "Granian shutdown completed, see ya!"); the reporter's 9th line ([ERROR] Unexpected exit from worker-1) is a SIGTERM-timing variant. stderr empty. Capture: prerelease-testing/2026-08-27-v0.9.9a1/logging_cli/logs/verify_run_json_debug.stdout (validated with check_jsonlines.py). Counter-check at 

## FINDING-014: rx.Model subclass with table=True gives bare TypeError instead of 'pip install reflex[db]' guidance when sqlmodel is absent (LOW)

- Cluster: `pydantic_optional` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: In a venv with reflex==0.9.9a1 (no db extra): python -c "import reflex as rx
class Item(rx.Model, table=True): name: str" -> TypeError: Item.__init_subclass__() takes no keyword arguments. The helpful ImportError ('Database is not available. Please install ... reflex[db]') only triggers on instantiation via _ClassThatErrorsOnInit.__init__, but users hit __init_subclass__ first because table=True is the standard declaration form. Same on 0.9.8, but the pydantic-optional change makes minimal installs more common so more users will hit it.
- Evidence: reflex/model.py: _ClassThatErrorsOnInit only overrides __init__; Model falls back to it when sqlmodel missing. Probe output in prerelease-testing/2026-08-27-v0.9.9a1/pydantic_optional/NOTES.md section (a).
- Root cause (verifier analysis): reflex/model.py: when sqlmodel/sqlalchemy are absent, Model is aliased to _ClassThatErrorsOnInit, which overrides only __init__; a subclass declaration passing class keywords (table=True) routes them to object.__init_subclass__, which accepts no kwargs, raising a bare TypeError at class-definition time before the guided "pip install reflex[db]" ImportError path (_print_db_not_available) is ever reached.
- Verification notes: Reproduced from repro steps alone in a fresh PyPI-only venv (reflex==0.9.9a1, py3.11, no extras; sqlmodel/sqlalchemy/pydantic verified absent; run from neutral cwd so the local checkout could not shadow the install): `class Item(rx.Model, table=True): name: str` -> TypeError: Item.__init_subclass__() takes no keyword arguments, with no reflex[db] guidance. Refutation attempts failed: (1) not an environment quirk — deterministic class-machinery behavior, root cause verified in reflex/model.py (Model = _ClassThatErrorsOnInit which only overrides __init__, so class kwargs hit object.__init_subclass__ before the helpful ImportError can fire; plain subclassing is even silently accepted and only e

## FINDING-015: Prod vite build logs 'Invalid input options: jsx key' and 'advancedChunks deprecated' warnings on every build (LOW)

- Cluster: `pydantic_optional` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: reflex run --env prod --loglevel debug on any 0.9.9a1 app (vite 8.2.0 pinned); warnings appear twice per build (client and ssr environments).
- Evidence: pyd_prod_run.log lines: 'Warning: Invalid input options (1 issue found) - For the "jsx". Invalid key: Expected never but received "jsx".' and 'WARN advancedChunks option is deprecated, please use codeSplitting instead.'
- Root cause (verifier analysis): packages/reflex-base/src/reflex_base/compiler/templates.py (release branch lines ~736-744, identical at v0.9.8) emits `build.rollupOptions.jsx: {}` and `build.rollupOptions.output.advancedChunks` into every generated .web/vite.config.js; rolldown-vite 8.x no longer accepts `jsx` as a rollupOptions key and deprecates `advancedChunks` in favor of `codeSplitting`. Fix shape: drop `jsx: {}` and migrate advancedChunks -> codeSplitting in the template.
- Verification notes: CONFIRMED as a genuine, actionable framework defect — but PRE-EXISTING on 0.9.8, NOT a 0.9.9a1 regression; severity low/cosmetic. Independent repro from steps alone: fresh minimal app (verify1_prod_warnings/vpapp) + PyPI-only reflex==0.9.9a1 venv, `reflex run --env prod --frontend-port 8804 --backend-port 8804 --loglevel debug` -> vite v8.2.0 logs 'Warning: Invalid input options ... Invalid key: Expected never but received "jsx"' and 'WARN advancedChunks option is deprecated, please use codeSplitting instead' exactly twice each (client + ssr build environments); app still builds and serves HTTP 200. Repro nit: prod mode requires frontend-port == backend-port or reflex exits immediately. Refu

## FINDING-016: reflex.testing (AppHarness) requires uvicorn and psutil that the published wheel does not declare (LOW)

- Cluster: `registration_context` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: uv venv /tmp/v && uv pip install --python /tmp/v/bin/python --prerelease=allow 'reflex==0.9.9a1' && /tmp/v/bin/python -c 'import reflex.testing' -> ModuleNotFoundError: No module named 'uvicorn' (reflex/testing.py:28 top-level import); AppHarness.stop() also imports psutil.
- Evidence: Hit while setting up the harness tests in a clean PyPI venv (seq_test.log first run, exit 4). Same on 0.9.8 — not a regression, but any downstream user following AppHarness docs against the published package hits it.
- Root cause (verifier analysis): reflex/testing.py ships in the wheel with a top-level `import uvicorn` (line 28) and an unconditional `import psutil` inside AppHarness.stop() (line 473), but uvicorn and psutil are declared only in the repo's dev dependency-group (psutil additionally as a win32-only runtime dep), so the published wheel's Requires-Dist never includes them and no `testing` extra exists — an oversight dating back to at least 0.9.8, likely from when the framework's own test suite (which always installs the dev group) was the only consumer of AppHarness.
- Verification notes: CONFIRMED (low severity, pre-existing — not a 0.9.9a1 regression). Independently reproduced from the repro steps alone in fresh PyPI-only venvs under /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_registration_context_2/ (venvs v99 and v98): (1) clean `reflex==0.9.9a1` venv -> `python -c 'import reflex.testing'` fails with ModuleNotFoundError: No module named 'uvicorn' at reflex/testing.py:28 (top-level import), exactly as claimed; (2) `reflex==0.9.8` fails identically at the same line, confirming the claimant's "not a regression" framing; (3) psutil half verified empirically: after installing only uvicorn, `import reflex.testing` succeeds, and `A

## FINDING-017: compile_app() silently discards user/app-level bundle_library() registrations, and the dynamic serializer never rewrites subpath imports of bundled libs (LOW)

- Cluster: `registration_context` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: App calling bundle_library('lucide-react') at module import + a computed rx.Component var containing rx.icon(...): window.__reflex never contains lucide-react (reset_bundled_libraries() at reflex/compiler/compiler.py ~1209 wipes it, re-adding only plugin deps); serialization passes whose context DOES hold the lib emit un-rewritten bare specifiers like `from "lucide-react/dist/esm/icons/apple.mjs"` inside the eval'd data-URI module -> TypeError: Failed to resolve module specifier in the browser (dynamic.py rewrite loop matches only exact `from "<lib>"`). See dynapp/ app + drive_dynapp.py.
- Evidence: dynapp/run_dev.log and dynapp098_run_dev.log each contain 2x 'Failed to resolve module specifier' — identical on 0.9.8, so pre-existing, but worth fixing while bundled_libraries plumbing is being reworked; it defeats the documented-for-plugins bundle_library pattern for any lib imported via subpaths.
- Root cause (verifier analysis): Two compounding pre-existing defects in the bundled-libraries plumbing (relocated but unchanged by PR #6382): (a) compile_app() calls reset_bundled_libraries() at the start of every frontend compile and re-adds ONLY plugin.get_frontend_dependencies(), so any user/app-level bundle_library() call made at module import is silently dropped from window.__reflex — while the backend worker short-circuits compile_app before the reset and KEEPS the registration, leaving frontend bundle and backend registry permanently out of sync; (b) make_component's window-rewrite loop matches only the exact substring `from "<lib>"`, so deep/subpath imports (rx.icon's package_path form `lucide-react/dist/esm/icons/*.mjs`, where format_library_name strips only @version) of a lib that IS in the registry are kept as bare specifiers inside the eval'd data-URI module, which browsers can never resolve (no import map applies) -> TypeError: Failed to resolve module specifier.
- Verification notes: CONFIRMED (low severity; explicitly NOT a 0.9.9a1 regression — byte-identical on 0.9.8). Reproduced independently from the repro steps with my own app/driver/venvs (no reuse of claimant artifacts) in /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_registration_context_3/, ports 3932/8932. (1) Serializer-level, zero network: with 'lucide-react' registered, serialize(rx.vstack(rx.icon("apple"))) emits bare `import LucideApple from "lucide-react/dist/esm/icons/apple.mjs"` — the rewrite loop in reflex_base/components/dynamic.py (~line 128 in the 0.9.9a1 wheel) only matches the exact `from "<lib>"` substring with closing quote, and format_library_name s

## FINDING-018: Failed REFLEX_USE_NPM run persists an inconsistent reflex.lock (npm<->bun switch breaks subsequent runs) (LOW)

- Cluster: `prod_export` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: 1) Run once with REFLEX_USE_NPM=1 and an old-node shim so the run fails on the version check (previous issue) - despite the failure, .web/package-lock.json is created and {bun.lock, package-lock.json, package.json} are copied into reflex.lock/. 2) Run normally (bun, real node): bun install --frozen-lockfile fails with 'error: lockfile had changes, but lockfile is frozen' and reflex aborts telling the user to delete reflex.lock.
- Evidence: logs/oldnode_bun.log (failure + reflex's actionable message); reflex.lock.outofsync-evidence/ contains the inconsistent persisted trio. Recovery advice works: after 'rm -rf reflex.lock' the next run regenerates a clean bun-only lock and the app serves (logs/recovery_run.log). Low severity because the message is actionable, but a run that failed preflight should not have mutated the persisted lockfile state at all.
- Root cause (verifier analysis): install_frontend_packages never invalidates the other package manager's persisted lockfile on a manager switch: an npm-primary install rewrites .web/package.json pins to caret ranges (npm's default save-prefix) and creates package-lock.json, then sync_web_lockfiles_to_root persists them ALONGSIDE the now-stale exact-spec bun.lock; on the next run _persisted_lockfile_implies_npm() returns False because bun.lock exists, so bun runs 'install --frozen-lockfile' against a package.json that no longer matches bun.lock's recorded workspace specs and correctly refuses. Exposed in 0.9.9a1 by removing the postcss entry from PackageJson.OVERRIDES, which previously made the npm path fail (EOVERRIDE) before any lockfile persistence. Fix directions: on npm-primary installs use --save-exact and/or drop/regenerate bun.lock (and vice versa), or prefer npm when reflex.lock carries a package-lock.json newer than bun.lock.
- Verification notes: Reproduced end-to-end on 0.9.9a1 from the repro steps alone (fresh blank app, smoke venv, ports 3968/8968; evidence: prerelease-testing/2026-08-27-v0.9.9a1/prod_export/verification/lockfile_verify/ and VERIFICATION section appended to that dir's NOTES.md). (1) REFLEX_USE_NPM=1 + old-node shim: the fatal node-version error fires only AFTER _compile_app() has already run the full npm install (run2b log: install done at 6.57s, error 4 lines later), persisting {bun.lock, package-lock.json, package.json} into reflex.lock/ with package.json rewritten to caret ranges while bun.lock keeps exact specs. (2) Next plain bun run: 'bun install --frozen-lockfile' -> 'lockfile had changes, but lockfile is f

## FINDING-019: vite 8.2.0 bump introduces two new warnings on every prod build/export ('jsx' invalid input option; advancedChunks deprecated) (LOW)

- Cluster: `prod_export` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: reflex run --env prod (or reflex export) on 0.9.9a1; watch the react-router build output for the client and ssr builds.
- Evidence: logs/prod_run.log and logs/export.log: 'Warning: Invalid input options (1 issue found) - For the "jsx". Invalid key: Expected never but received "jsx".' and 'WARN advancedChunks option is deprecated, please use codeSplitting instead.' (each twice per build). Introduced by the 8.0.16->8.2.0 rolldown-vite bump; reflex's vite config passes a 'jsx' input option and uses advancedChunks, both flagged by the new version. Cosmetic today; advancedChunks is slated for removal so will eventually break.
- Root cause (verifier analysis): Reflex's own generated .web/vite.config.js passes `build.rollupOptions.jsx: {}` (not a valid rolldown input option key) and `build.rollupOptions.output.advancedChunks` (deprecated in favor of codeSplitting). Source: packages/reflex-base/src/reflex_base/compiler/templates.py (~line 736 on release branch origin/r/pre-2026.08.27-33148999938; identical block ~line 652 in tag reflex-base-v0.9.8). Both pinned rolldown-vite versions (8.0.16 and 8.2.0) warn identically — pre-existing template debt, not the version bump.
- Verification notes: Warnings CONFIRMED as real and framework-caused, but the claim's regression attribution is REFUTED. Independent repro (fresh `reflex init --template blank` apps, PyPI-only venvs): on 0.9.9a1 both `reflex export --loglevel debug` and `reflex run --env prod --frontend-port 8972 --backend-port 8972 --loglevel debug` log 'Invalid input options ... For the "jsx". Invalid key: Expected never but received "jsx"' and 'WARN advancedChunks option is deprecated, please use codeSplitting instead' exactly twice each (client+ssr vite builds); build output is unaffected (export exit 0, prod server serves HTTP 200). HOWEVER, the same app on reflex 0.9.8 (separate PyPI venv, vite 8.0.16) emits the IDENTICAL 

## FINDING-020: rx.plotly silently drops the id prop (react-plotly.js expects divId) (LOW)

- Cluster: `devtools_perf` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: In any 0.9.9a1 app: rx.plotly(data=<go.Figure var>, id='the-plot'); load the page; document.getElementById('the-plot') is null while the chart renders. See devtools_perf/probe_charts.py against the bundled devtools_app (/charts page).
- Evidence: probe_charts.py output: 'page-root exists: 1 / the-plot exists: 0' with the chart visibly rendered (out_prod/03_charts.png). react-plotly.js Plot only forwards divId to its container div; the Reflex wrapper passes id through as a React prop that Plot ignores. Almost certainly longstanding (wrapper API unchanged in this respect), not a 0.9.9 regression; a divId mapping or docs note would fix it.
- Root cause (verifier analysis): react-plotly.js 4.0.0's Plot component only forwards its `divId` prop (plus style/className/ref) to the container div it renders; the Reflex Plotly wrapper passes the framework-universal `id` prop through as a plain React prop, which falls into Plot's ...eventProps rest and is silently ignored. Longstanding third-party-API mismatch present identically in 0.9.8 (same pinned react-plotly.js@4.0.0), not a 0.9.9a1 regression.
- Verification notes: Reproduced independently on reflex 0.9.9a1 (fresh PyPI venv, minimal blank-template app, dev mode, ports 4000/9000): rx.plotly(data=..., id='the-plot') renders the chart but document.getElementById('the-plot') finds nothing; control rx.box(id='control-box') gets its id (not API misuse) and a static (non-state-var) figure drops the id too. Compiled JSX proves Reflex emits the prop (jsx(Plot,{id:"the-plot",ref:ref_the_plot,...})); react-plotly.js@4.0.0 dist source proves Plot destructures divId and renders its container as React.createElement("div",{id:divId,style,ref,className}), so id falls into ...eventProps and is silently discarded. Refutation attempts: (a) 0.9.8 baseline in a separate Py

## Refuted / reclassified claims

- **Prod mode serves HTTP 404 status for direct loads of dynamic-route URLs (pre-existing, not a 0.9.9 regression)** (`routing`): REPRODUCED EXACTLY AS CLAIMED, but not confirmable as an actionable 0.9.9a1 defect. Independent repro with a fresh minimal app (scratchpad apps/verify_routing_1/vapp, pages /, /other, /articles/[id], /posts/[[...splat]]) under 'reflex run --env prod' on port 8604: /articles/7, /articles/7/, /posts, /posts/a/b, /definitely-not-a-page all return HTTP 404 with bodies byte-identical (cmp) to .web/buil

- **PR #6863 claim 'import reflex bootstraps the reflex-owned loggers' is not literal: bootstrap happens on first framework use, not bare import** (`logging_cli`): Reproduced the reported behavior exactly, but it is not a framework defect a fix-agent should act on — it is an imprecise one-liner in the merged PR #6863 description only. Facts verified independently (smoke venv = PyPI reflex 0.9.9a1, clean cwd /tmp/.../scratchpad/apps/verify_logging_cli_2/, no server needed): (1) the exact repro prints 'False <RootLogger root (WARNING)>' — bare `import reflex` 



# Phase 1 cluster summaries (feature exploration, 11 clusters / 32 agents)

Each cluster dir under this directory contains the sample apps, Playwright drivers,
NOTES.md (with VERIFICATION appendices from independent verifier agents), and screenshots.
All installs were PyPI-only in isolated venvs; every app was driven end-to-end in Chromium
with console/network/server-log capture, in dev and (where relevant) prod modes, with
0.9.8 baselines where behavior differences mattered.

### `routing` (anomaly:5, pass:15)

Built one reflex 0.9.9a1 app covering all four routing changelog items and drove it end-to-end in Chromium in dev and prod modes, with a reflex 0.9.8 baseline app for comparison. All four fixes work: #6593 stale on_load chains are cancelled on navigation (0.9.8: they survive and keep writing state) and supersedes=True gives latest-wins on rapid clicks; #6790 the [[...splat]] catchall no longer matches prefix paths (0.9.8 reproduces the misfire: /posts-archive renders 404 but fires the catchall's on_load); #6953 /posts/all/[x] + /posts/[id] compile in both orders (0.9.8 raises RouteValueError in dynamic-first order) while genuine same-position name conflicts still raise; #6919 chained events resolve router/dynamic args against the view that produced them in both two-tab and same-tab navigation-race scenarios (0.9.8 resolves against the new view). Two adjacent observations: an on_load that is a background task is now cancelled on navigation (it survived on 0.9.8; button-started background tasks still survive), and prod mode serves HTTP 404 status for direct loads of dynamic-route URLs (pre-existing, identical on 0.9.8).

### `memo` (anomaly:4, pass:13)

Built a memo-cluster app (state-bound + EventHandler props, rx.foreach, client_state, ComponentState-in-memo, RestProp, wrapper=None, cross-module same-name memos, NoSSR moment, two routes) with render-count probes hoisted into each compiled rendering domain, and drove it in Chromium in dev and prod: 69/69 checks passed in both modes. #6949 is confirmed precisely — the page function never re-renders (compiled page has zero useContext; wrappers re-render per state context, inner memos only when prop values change; foreach rows re-render only for changed labels), #6605 font_weight= merges into css with explicit style= (computed 700/10px/italic), #6945 displayNames present in compiled output and at runtime in dev and minified prod, no hydration errors or failed requests. No framework bugs found; only a low-severity prod-build warning-noise issue plus informational naming observations.

### `state_concurrency` (anomaly:2, pass:12)

All checks pass on reflex 0.9.9a1 in dev and prod. The #6920 fix is confirmed end-to-end: hammering foreground clicks against background-task completions (20 rounds, async handler that awaits after its write, uncached async computed var widening the delta-resolution window) renders every single counter value in the browser on 0.9.9a1, while the identical app+driver on 0.9.8 loses exactly one foreground delta per background completion (20/20 rounds, values skipped every ~8 clicks — matching the changelog's "1 in 8" symptom). #6734's flush tick keeps sync-generator streaming perfectly incremental (10/10 interim values, ~142ms apart, identical to 0.9.8), #6830 lock isolation verified at script level, no-context background handlers still flush exactly one delta under the lock, and rx._x.client_state stayed consistent with racing server deltas throughout. No framework issues found.

### `client_error` (anomaly:2, fail:1, pass:9)

Exercised PR #6827's client_error socket event end-to-end on reflex 0.9.9a1 (PyPI) with a real Chromium browser and raw python-socketio. The feature works well: real compiled-frontend mismatches (synthetic ghost-substate emit, a background-task emit, and a genuine stale-frontend-vs-new-substate scenario) all produce an actionable browser-console error, an actionable backend terminal log, and a fatal session (further events blocked, no auto-reload); normal apps see no client_error. Backend hardening largely holds: client values are sanitized (ANSI/newlines/NUL/BEL/BS stripped, rich markup escaped, byte-verified), 500-char truncation works, a 10MB payload is dropped at the 1MB transport buffer, per-SID (5) and per-window (20) rate limits fire, and unknown-sid/non-dict payloads are gated. One real defect found: emitting client_error with NO data argument raises an unhandled TypeError (on_client_error's data param has no default) BEFORE the isinstance guard, unknown-sid gate, and rate limiters — an unauthenticated backend-log-spam vector that defeats the PR's own hardening.

### `logging_cli` (anomaly:5, fail:2, pass:14)

Exercised the 0.9.9a1 logging pipeline end-to-end from PyPI installs: LogLevel ordering/banner fix, JSON-lines mode (flag + env var) across init/export/run in dev and prod with real Chromium drives, console.* deprecation shims, library-mode logger hygiene, and the #6924 deploy/cloud fallbacks plus hosting-cli 0.1.71a2 compatibility (standalone and with reflex 0.9.8). Core features all work: JSON output is strictly parseable for reflex-owned logs (165/165 export records, 13/13 prod run records), shims still render with per-callsite-deduped deprecation warnings, and a missing hosting-cli yields helpful install messages. One real bug found: with REFLEX_ENABLE_FULL_LOGGING the granian worker's file handler is closed, silently dropping all worker records from the log file and spilling timestamped duplicate plain-text lines onto stdout — which breaks the --json contract. Lesser anomalies: granian's plain startup lines pollute --json stdout at debug level, and the "import reflex bootstraps loggers" claim from PR #6863 only holds on first framework use, not bare import.

### `pydantic_optional` (anomaly:3, pass:11)

Pydantic-optional (PR #6786) works as intended in 0.9.9a1: bare install ships without pydantic, imports nothing pydantic at startup, and a dataclass/dict app is fully functional in Chromium; reflex[pydantic] restores full pydantic v2 model support (mutations, nested models, foreach over list[Model], serialization, model-hinted event args) with behavior identical to 0.9.8 in dev and prod; reflex[db] CRUD via rx.session works end-to-end; in-place upgrade 0.9.8 to 0.9.9a1 keeps pydantic installed and the app running. No regressions found. Two pre-existing low-severity UX defects recorded: subclassing rx.Model with table=True without sqlmodel raises a bare TypeError with no install guidance, and a clean-install app importing pydantic gets a plain ModuleNotFoundError without a reflex[pydantic] hint (fails fast at 0.83s though). Adjacent: prod vite build logs invalid-option/deprecation warnings on every build.

### `typing_python` (anomaly:3, fail:2, pass:8)

PEP 695 alias support (#6944) works at compile/render time on Python 3.11 (backport)/3.12/3.13/3.14 — all alias shapes produce correct Var kinds and render in the browser, where 0.9.8 cannot even import the app — but two TypeAliasType coverage gaps remain: runtime assignment to any alias-annotated state var raises TypeError in State.__setattr__ (every mutating event handler silently dies, making the feature unusable for mutable vars), and passing an alias-annotated handler uncalled to an event trigger crashes page compile. Python 3.14 builtin-annotation handling (#6890/#6896) passed fully end-to-end (dict/list/set/tuple/bool payloads, background task, event chain, upload) with a 3.11 control run identical; pyright-as-user shows correct Var.create inference (#6846) with 0 errors, though 0.9.8 also passes under pyright 1.1.413 so that fix could not be differentiated. 3.13 import/compile smoke passed.

### `components` (anomaly:4, fail:1, pass:15)

Exercised all 6 component-level changelog items in 0.9.9a1 end-to-end (real reflex server + Chromium via Playwright, plus raw httpx multipart) with a single two-page app. All fixes work: code_block custom_style tracks backend/client/memo state vars and wrap_long_lines merges whiteSpace:pre-wrap without clobbering user code_tag_props (#6520); rx.script executes reliably 10/10 reloads via synchronous Helmet flush (#6905); deprecated App(theme=...) applies crimson accent in dev AND prod with an explicit RadixThemesPlugin (#6776); buffered uploads sanitize hostile filenames identically to streamed — 0.9.8 let ../../evil.txt escape the upload dir, 0.9.9a1 contains it (#6753); REFLEX_REFERRER_PARAM urlencodes into the prod-only badge href (#6951); ToastProps.dict works (#6846). One medium issue: the new _sanitize_upload_filename still returns the traversal token ".." for all-dots filenames (.., ./../., ..\, /..), pointing get_upload_dir()/file.path at the upload dir's parent and producing an unhandled IsADirectoryError 500 — a sanitizer-contract gap within #6753's scope (not a regression; 0.9.8 behaved the same).

### `registration_context` (anomaly:7, pass:7)

RegistrationContext multi-app isolation works as designed on 0.9.9a1: two rx.App instances coexist via fork() with zero page/state/config/bundled-library leakage (11/11 checks), and AppHarness runs twice in one pytest process — sequentially AND simultaneously with different app sources — serving each app's own pages and routing events to the right State with no cross-talk (verified end-to-end in Chromium). The dynamic-component/bundle_library flow is byte-for-byte identical to 0.9.8 in dev and prod (no regression; pre-existing dev-mode CDN fallback and subpath-import sharp edges preserved), and AppHarness-hosted dynamic components render with zero CDN traffic. Breaking-API surface: get_config(reload=True) now dies with a bare TypeError that never mentions reload_config(), bundled_libraries module attr raises bare AttributeError (corroborates FINDING-001), and two UNdocumented breaks were found: reflex.page.DECORATED_PAGES is gone (with a confusing ImportError naming 'PageNamespace') and a second bare rx.App() in one process now raises ReflexRuntimeError where 0.9.8 silently allowed it.

### `prod_export` (anomaly:5, pass:10, skipped:1)

Built a multi-page app (dynamic route, state events, event chains, foreach/cond, custom library="react-router" components) and ran full Playwright browser passes in prod, exported-static+backend-only, preview, and dev modes: all 18 checks pass in every mode with clean hydration and correct REFLEX_ENV_MODE; prod prerenders / and /about; 'reflex export' succeeds with no ECONNREFUSED and REFLEX_API_URL is baked correctly; .web/package.json has react-router 8.3.0, vite 8.2.0, no react-router-dom, no postcss override. Main issues found: a component declaring library="react-router-dom" silently installs react-router-dom@^7.18.2 in dev (works by accident, no warning) but breaks prod builds with a cryptic prerender TypeError; 'reflex run' hangs forever after the fatal node-version error on the REFLEX_USE_NPM path; and a failed npm run leaves reflex.lock out of sync so subsequent bun runs fail (with an actionable recovery message that works).

### `devtools_perf` (anomaly:2, pass:12)

Built a 4-page app (custom component classes, @rx.memo functions, nested substates, rx.plotly NoSSR wrapper, dynamic route blog/[slug], ~1600-element heavy page) on reflex 0.9.9a1 from PyPI and drove it in Chromium across dev-default, dev+REFLEX_REACT_OWNER_STACKS=1, and prod. #6945 naming is fully verified at runtime via React fiber walks (pages labelled with routes incl. Component(blog/[slug]), memo components carry Python names, all contexts named incl. nested-substate StateContext(...) paths, plotly shows ClientSide(Plot)), and #6905 behaves exactly as documented: captureOwnerStack() returns no frames in default dev (internals counter pinned at 1e9, setter no-op) and real frames with the env var set; CDP metrics show heavy-page navigation at ~100ms task CPU by default vs ~338ms with owner stacks restored (~68ms prod), matching the changelog's claimed magnitudes. No framework bugs found; one low-severity longstanding wrapper quirk (rx.plotly drops id=).

# Phase 2 confirmed findings (upgrade + enterprise, verified independently)

## FINDING-021: reflex-enterprise 0.9.4 + reflex 0.9.9a1: any python-callable ag-grid renderer/formatter whose compiled return expr carries imports crashes app compile (FINDING-001 end-to-end); real AttributeError masked as 'VarAttributeError: Attribute _cached_get_all_var_data not found' (CRITICAL)

- Cluster: `ent_aggrid` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: cd prerelease-testing/2026-08-27-v0.9.9a1/ent_aggrid/minimal; uv venv venv --python 3.11 && uv pip install --python venv/bin/python --prerelease=allow 'reflex==0.9.9a1' 'reflex-enterprise==0.9.4'; REPRO_LAMBDA=1 CI=1 REFLEX_TELEMETRY_ENABLED=false venv/bin/reflex run --loglevel debug --frontend-port 3302 --backend-port 8302 -> exits at 'Compile pages' with VarAttributeError. Same app with reflex==0.9.8 runs and renders (shots_minimal/min_lambda_098.png). Full demo A/B: demo_098 (17/17 routes E2E pass) vs demo_a1 (compile dead).
- Evidence: artifacts/traceback_minimal_a1.txt and artifacts/traceback_demo_a1_masked_varattributeerror.txt. Root cause chain (verified by patching cached_property.__get__): reflex_enterprise/vars.py:143 'set(dynamic.bundled_libraries)' -> AttributeError (attr removed by #6382) raised inside CachedVarOperation._cached_get_all_var_data computation -> descriptor AttributeError makes Python fall back to Var.__getattr__ -> bogus VarAttributeError, original error absent from output. Direct proof: LiteralLambdaVar.create(flag_formatter) raises "AttributeError: module 'reflex.components.dynamic' has no attribute
- Root cause (verifier analysis): reflex-base 0.9.9a1 PR #6382 (commit 7888170ad, 'Move more globals to RegistrationContext') removed the public module-level list reflex_base.components.dynamic.bundled_libraries (star-re-exported as reflex.components.dynamic.bundled_libraries) without a deprecation shim; reflex-enterprise 0.9.4's LiteralLambdaVar._validate_and_extend_return_expr (reflex_enterprise/vars.py:143) reads it when compiling any python-callable ag-grid renderer/formatter whose return expression carries imports, raising AttributeError; that AttributeError escapes reflex_base's custom cached_property.__get__ during _cached_get_all_var_data computation, triggering the __getattr__ fallback which re-raises it as the misleading 'VarAttributeError: Attribute _cached_get_all_var_data not found' with the original cause swallowed.
- Verification notes: CONFIRMED as a genuine critical regression — attribution: reflex 0.9.9a1 (reflex-base) breaking change, not an rxe bug, env quirk, app bug, or misuse. Reproduced from scratch with fresh PyPI venvs (python 3.11) in an independent working dir (/tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_ent_aggrid_0/, ports 3800/8800 and 3801/8801). Evidence: (1) direct offline proof — on reflex==0.9.9a1 + reflex-enterprise==0.9.4, LiteralLambdaVar.create(lambda params: rx.text(...)) raises AttributeError: module 'reflex.components.dynamic' has no attribute 'bundled_libraries' at reflex_enterprise/vars.py:143 (probe_direct.py); (2) end-to-end — same minimal app w

## FINDING-022: FINDING-001 (dnd/LambdaVar surface): non-static LambdaVar prop with bundle_library imports crashes with AttributeError at reflex_enterprise/vars.py:143 on 0.9.9a1 (HIGH)

- Cluster: `ent_map_dnd` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: uv venv v --python 3.11 && uv pip install --python v/bin/python --prerelease=allow 'reflex==0.9.9a1' 'reflex-enterprise==0.9.4'; then run prerelease-testing/2026-08-27-v0.9.9a1/ent_map_dnd/repro_finding001_dnd_can_drop.py -> AttributeError: module 'reflex.components.dynamic' has no attribute 'bundled_libraries' (vars.py:143, full traceback in ent_map_dnd/logs/repro_finding001_099a1.log). Same script on 'reflex==0.9.8' exits 0.
- Evidence: Precise trigger mapping for the map/dnd portion of FINDING-001: LiteralLambdaVar.create runs _validate_and_extend_return_expr (the only reader of dynamic.bundled_libraries) ONLY for non-@rxe.static callables whose return expression carries VarData without hooks. Hence the SHIPPED demos are unaffected on 0.9.9a1 (map: no LambdaVar props at all — grep of the wheel shows only components/dnd/dnd.py uses LambdaVar; dnd: kanban can_drop funcs are @rxe.static, basic/foreach pass none) and both demos pass full Playwright e2e (13/13 and 18/18) identical to 0.9.8. The realistic user path that DOES crash
- Root cause (verifier analysis): reflex-base 0.9.9a1 PR #6382 moved the module-global `bundled_libraries` list from `reflex_base.components.dynamic` onto `RegistrationContext.ensure_context().bundled_libraries` without leaving a deprecated module attribute; reflex-enterprise <=0.9.4 `LiteralLambdaVar._validate_and_extend_return_expr` (vars.py:143) still does `set(dynamic.bundled_libraries)` for non-static lambda props whose return expression carries import VarData, so it raises AttributeError at component construction.
- Verification notes: CONFIRMED — reflex-enterprise incompatibility (not an accidental reflex defect): reflex-base 0.9.9a1 (PR #6382 "Move more globals to RegistrationContext") deliberately removed the module-level `bundled_libraries` list from `reflex_base.components.dynamic` (re-exported as `reflex.components.dynamic`) with no `__getattr__`/alias shim, while reflex-enterprise 0.9.4 — the NEWEST enterprise release on PyPI, whose dependency `reflex[db]>=0.9.6` has no upper bound, so co-install is a legal/default resolution — still reads `dynamic.bundled_libraries` at vars.py:143 in `LiteralLambdaVar._validate_and_extend_return_expr`. Independently reproduced in fresh PyPI-only venvs (/tmp/.../scratchpad/apps/veri

## FINDING-023: 0.9.9a1 removes reflex.page.DECORATED_PAGES (PR #6382) with no shim and no changelog mention — breaks reflex-enterprise flow demo at import with a confusing error (HIGH)

- Cluster: `ent_misc` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: uv venv v --python 3.11 && uv pip install --python v/bin/python --prerelease=allow 'reflex==0.9.9a1'; v/bin/python -c "from reflex.page import DECORATED_PAGES" -> ImportError: cannot import name 'DECORATED_PAGES' from 'PageNamespace' (unknown location). Same command on reflex==0.9.8 succeeds (defaultdict). End-to-end: reflex-enterprise/demos/flow crashes at compile on 0.9.9a1 (flow/flow.py:2); the unmodified demo runs fully on 0.9.8 (11/11 browser checks, see ent_misc/NOTES.md + drive_flow.py).
- Evidence: 0.9.8 reflex/page.py kept DECORATED_PAGES as a class attr on the PageNamespace that replaces the module in sys.modules; 0.9.9a1 page.py drops it — pages now accumulate on RegistrationContext.ensure_context().decorated_pages (packages/reflex-base/news/6382.feature.md). 6382.breaking.md documents the bundled_libraries and get_config(reload=True) changes but NOT this removal. Downstream fix is trivial (documented patch in ent_misc/flow_099/flow/flow.py works: entries are the same (render_fn, kwargs) tuples), but a compat attr or at least a changelog entry is warranted; the 'PageNamespace (unknown
- Root cause (verifier analysis): PR #6382 (commit 7888170ad, "Move more globals to RegistrationContext") deleted the module-level DECORATED_PAGES defaultdict from reflex/page.py, its append in the page() decorator, AND the deliberate `DECORATED_PAGES = DECORATED_PAGES` class re-export on PageNamespace, moving page registration to RegistrationContext.ensure_context().decorated_pages — without a __getattr__/deprecation shim. The confusing "from 'PageNamespace' (unknown location)" ImportError text comes from the pre-existing `sys.modules[__name__] = page_namespace` module replacement in page.py. The release process then under-documented it: 6382.breaking.md covers only get_config(reload=True) and bundled_libraries, and the released reflex-base CHANGELOG files the RegistrationContext scoping under Features.
- Verification notes: CONFIRMED — this is a reflex 0.9.9a1 defect (undocumented breaking API removal with no shim and a confusing error), manifesting downstream as a reflex-enterprise DEMO/TESTS incompatibility (the published reflex_enterprise 0.9.4 library itself has zero DECORATED_PAGES references, so enterprise runtime is unaffected — unlike FINDING-001). Independent repro in fresh PyPI venvs (uv, python 3.11, cwd outside the checkout) at /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_ent_misc_0/: (1) `v991/bin/python -c "from reflex.page import DECORATED_PAGES"` fails on 0.9.9a1 with exactly `ImportError: cannot import name 'DECORATED_PAGES' from 'PageNamespace' (u

## FINDING-024: reflex-base CachedVarOperation masks any AttributeError raised inside cached var computations, producing a misleading unchained 'Attribute _cached_get_all_var_data not found' (MEDIUM)

- Cluster: `ent_aggrid` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Any Var whose _cached_get_all_var_data computation raises AttributeError (e.g. rxe LiteralLambdaVar on 0.9.9a1): compile traceback ends in reflex_base/vars/base.py:1472 VarAttributeError with no __cause__; the real error is invisible. Reproduce via minimal REPRO_LAMBDA app or: patch reflex_base.vars.base.cached_property.__get__ to print swallowed AttributeErrors and re-run.
- Evidence: cached_property.__get__ (reflex_base/vars/base.py ~2052-2075) lets AttributeError from the wrapped function escape; Python's attribute machinery then discards it and calls CachedVarOperation.__getattr__ (base.py:2104) -> Var.__getattr__ raises VarAttributeError. Diagnosing the critical issue above required runtime instrumentation. Suggested: wrap the cached call and re-raise AttributeError as a chained RuntimeError. Longstanding pattern (same on 0.9.8), but it turned this release's headline enterprise breakage into an undebuggable message.
- Root cause (verifier analysis): reflex_base/vars/base.py cached_property.__get__ (line ~2076) calls the wrapped computation with no exception handling; when the computation raises AttributeError, Python's attribute machinery discards it (PyErr_Clear before the __getattr__ fallback) and CachedVarOperation.__getattr__ -> Var.__getattr__ re-raises an unchained VarAttributeError 'Attribute _cached_* not found.' — and on Mapping-typed ObjectVars the fallback instead silently fabricates an ObjectItemOperation Var. Fix: catch AttributeError inside cached_property.__get__ (or the cached wrapper) and re-raise chained as a non-AttributeError (e.g. RuntimeError) so real failures are never converted into bogus attribute-lookup misses.
- Verification notes: CONFIRMED as a genuine reflex (reflex-base) defect — a diagnosability/error-handling bug, NOT a reflex-enterprise incompatibility and NOT a regression (reproduced identically on reflex-base 0.9.8, matching the claim's "regression: false"; severity medium is fair). Independently reproduced two ways with fresh processes in PyPI-only venvs (reflex 0.9.9a1+rxe 0.9.4 and reflex 0.9.8+rxe 0.9.4, both verified index installs): (1) pure reflex-base probe (no enterprise) — a CachedVarOperation subclass declared exactly like reflex-base's own ConcatVarOperation whose _cached_get_all_var_data computation raises AttributeError surfaces as VarAttributeError 'Attribute _cached_get_all_var_data not found.'

## FINDING-025: shipped rxe ag_grid demo is broken against reflex >= 0.9.8 regardless of 0.9.9a1: bundle_library('$/utils/components') is a stale pre-0.9.8 memo path (MEDIUM)

- Cluster: `ent_aggrid` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Run demos/ag_grid unmodified with reflex==0.9.8 + rxe 0.9.4: compile fails 'ValueError: Library $/app_components/ag_grid/formatters is not bundled'. Adding the correct path while KEEPING the stale one makes compile pass but every route 500s (vite: Cannot find module '$/utils/components' in root.jsx). Working fix in demo_098/ag_grid/formatters.py: bundle '$/app_components/ag_grid/formatters', drop '$/utils/components' (applied at import AND page-eval time; note compile_app resets bundled_libraries after app import on 0.9.8, reflex/compiler/compiler.py:1202, so rxe's error-message advice of import-time bundle_library is unreliable).
- Evidence: artifacts/traceback_demo_098_shipped_bundle_path.txt; logs/run_098_shipped_crash.log; the working post-fix run logs/run_098_working.log with 17/17 routes passing. Related runtime desync on 0.9.8: granian worker (which never runs compile_app/plugin bundling) logs '[Reflex Backend Exception] ValueError: Library @radix-ui/themes is not bundled' whenever it serializes a state var holding lambdas (artifacts/backend_exception_098_state_lambda_serialization.txt) - the formatters State-tab defs never reach the client even on 0.9.8.
- Root cause (verifier analysis): reflex-enterprise 0.9.4 predates reflex's memo output-path mirroring (#6457, shipped v0.9.6): its demo bundles the legacy "$/utils/components" library while @rx.memo components now compile to "$/app_components/<module path>", and rxe's LiteralLambdaVar bundling validation reads a bundled-libraries list whose lifecycle (reset by compile_app at reflex/compiler/compiler.py:1202; never populated with plugin deps in backend worker processes) makes both rxe's import-time-bundling advice and its runtime serialization validation unreliable.
- Verification notes: CONFIRMED as a reflex-enterprise incompatibility with reflex >= 0.9.8 (NOT a reflex 0.9.9a1 defect; not a regression vs 0.9.8 — agrees with the claim's "regression: false"). Independently reproduced from scratch: fresh PyPI venv (reflex==0.9.8, reflex-base==0.9.8, reflex-enterprise==0.9.4, py3.11), pristine copy of /home/user/reflex-enterprise/demos/ag_grid, ports 3808/8808. (1) Unmodified shipped demo: compile fails exit 1 with "ValueError: Library $/app_components/ag_grid/formatters is not bundled" — the demo's bundle_library("$/utils/components") at formatters.py:16 is stale; on 0.9.8 the row_counter memo's library is $/app_components/ag_grid/formatters (mirrored memo layout, reflex #6457

## FINDING-026: reflex-enterprise HTTPCookie.sync(): /_reflex/cookies/sync endpoint 404s in the OIDC demo (route registration never reaches the serving backend worker) — broken on 0.9.8 too (MEDIUM)

- Cluster: `ent_misc` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Run reflex-enterprise/demos/oidc (any provider env), open the app, click 'Cookie Sync' -> browser console error, POST http://localhost:<backend-port>/_reflex/cookies/sync returns 404. Reproduced identically on reflex 0.9.9a1 and 0.9.8 with reflex-enterprise 0.9.4 (ent_misc/shots/shots_oidc_099 and shots_oidc_098 results.json req_failures).
- Evidence: reflex_enterprise/auth/cookie.py ensure_handlers_registered() inserts Route('/_reflex/cookies/sync', ...) into get_app().app._api.routes when HTTPCookie.sync() builds the EventSpec at compile time; in this demo shape (deprecated register_auth_endpoints(), no rxe.AuthPlugin) the insertion does not land in the process/app instance actually serving the backend, so the endpoint is absent. Enterprise-side, pre-existing — filed for the reflex-enterprise team rather than as a 0.9.9a1 blocker.
- Root cause (verifier analysis): reflex-enterprise registers /_reflex/cookies/sync only as a side effect of HTTPCookie.sync() building an EventSpec during page compilation; with reflex's split-process serving (compile in the CLI process, backend served by a granian worker that skips compile via the .nocompile marker), the Route insertion into get_app().app._api.routes lands in the compile process's app instance, never the serving worker's. Fix belongs in reflex-enterprise: register the route at backend startup in the serving process (e.g. AppEnterprise init or a backend plugin hook) rather than at EventSpec build time.
- Verification notes: CONFIRMED as a genuine reflex-enterprise 0.9.4 library defect — NOT a reflex 0.9.9a1 defect and NOT a regression (reproduced identically on 0.9.8), so nothing for the 0.9.9a1 release to act on; it belongs to the reflex-enterprise team, exactly as the original agent filed it. Independent repro (fresh PyPI venvs, pristine demos/oidc copy, mock IdP :8885, ports 3884/8884 and 3886/8887, working dir $SB/apps/verify_ent_misc_1): Chromium click on "Cookie Sync" -> POST /_reflex/cookies/sync -> 404 on both versions; direct curl 404 with AND without X-Reflex-Client-Token (decisive: when the route exists, a token-less POST returns 400 "No client token in request", never 404); /ping 200 rules out env/p

## FINDING-027: 0.9.9a1 console warnings print literal backslash-escaped brackets (dict\[str, ...]) (LOW)

- Cluster: `up_counter_todo` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: Run the todo example (event handler annotated dict[str, str] bound to on_submit) with reflex 0.9.9a1, --loglevel debug, log to file; grep for 'on_submit expects'. Compare with a 0.9.8 run of the same app.
- Evidence: logs/todo-0.9.9a1-run1.log line ~151 shows 'expects (dict\[str, typing.Any])'; logs/todo-0.9.8-run1.log line ~149 shows the same warning as 'expects (dict[str, typing.Any])' without backslashes.
- Root cause (verifier analysis): 0.9.8 built the warning with rich-markup escaping (.replace("[", "\\[")) and emitted it via console.warn, where rich consumed the \[ escapes. In 0.9.9a1 the call site in reflex_base/event/__init__.py was migrated to the new logging pipeline (logging.getLogger(__name__).warning) but kept the escaping; RichConsoleHandler.emit in reflex_base/utils/log.py renders records with markup=False unless extra={"rich": True} is passed (it is not), so the leftover backslash escapes print literally.
- Verification notes: CONFIRMED as a genuine reflex 0.9.9a1 defect (reflex-base package, NOT reflex-enterprise): cosmetic regression vs 0.9.8 in the compile-time EventHandler arg-mismatch warning. Independently reproduced from fresh copies of the todo app (handler annotated dict[str, str] bound to on_submit), PyPI installs only: 0.9.9a1 ($SB/envs/smoke) prints 'expects (dict\[str, typing.Any]) -> () but got (dict\[str, str])' with literal backslashes (byte-verified via cat -A, log $SB/apps/verify_up_counter_todo_0/a1-run.log:73), while 0.9.8 ($SB/envs/base098) prints clean brackets (098-run.log:72). Refuted alternatives: not an env quirk (fresh dirs, same machine, both versions); not app misuse (the warning itsel

## FINDING-028: New vite warning on every dev start: template vite.config.js imports vite-plugin-safari-cachebust without file extension (unsupported by future configLoader:'native' default) (LOW)

- Cluster: `up_counter_todo` | Regression vs 0.9.8: True | Verifier: CONFIRMED
- Repro: Run any app on reflex 0.9.9a1 with --loglevel debug; grep server log for 'VITE_CONFIG_NATIVE_IGNORE_WARNING'.
- Evidence: logs/counter-0.9.9a1-run1.log and logs/*-coldrun.log contain the warning twice per start; absent from logs/*-0.9.8-run1.log (vite 8.0.16). Introduced by the reflex web template + vite 8.2.0 bump.
- Root cause (verifier analysis): Template bug surfaced by dependency bump: reflex-base's vite.config.js template (packages/reflex-base/src/reflex_base/compiler/templates.py:658) imports "./vite-plugin-safari-cachebust" without a file extension; the pre-existing extensionless import became a warning when 0.9.9a1 bumped the pinned vite from 8.0.16 to 8.2.0 (which added the configLoader:'native' future-default check). Already fixed upstream post-release in commit 522cf410a (adds ".js"), not included in 0.9.9a1.
- Verification notes: CONFIRMED — genuine reflex 0.9.9a1 defect (reflex-base web template), not an enterprise incompatibility, env quirk, app bug, or misuse. Regression vs 0.9.8 in observable behavior: true (new warning on every dev start). Severity: low today (warning only, app fully functional), but the flagged import will hard-break when vite makes configLoader:'native' the default.

Independent repro (workdir /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_up_counter_todo_1/): (a) 0.9.9a1 (envs/smoke) todo app, `reflex run --loglevel debug` on 3604/8604 -> warning block printed twice before "App running at", frontend HTTP 200 (a1_todo_run.log); (b) 0.9.8 (envs/base0

## FINDING-029: reflex-enterprise 0.9.4 error paths emit console.error DeprecationWarning on 0.9.9a1 (app.py:120 login gate, utils.py:119 prod gate) (LOW)

- Cluster: `ent_map_dnd` | Regression vs 0.9.8: False | Verifier: CONFIRMED
- Repro: Run any rxe.App demo on 0.9.9a1 without CI/REFLEX_ACCESS_TOKEN (dev) or with --env prod: before the gate message, 'DeprecationWarning: console.error has been deprecated in version 0.9.9 ...' is printed pointing into reflex_enterprise code. Logs: ent_map_dnd/logs/map_099a1_run1.log, logs/dnd_099a1_prod.log.
- Evidence: console.error was deprecated in reflex-base 0.9.9; latest published enterprise still calls it, so enterprise users on 0.9.9a1 see deprecation noise attributed to a package they cannot fix. Cosmetic, but another signal that reflex-enterprise needs a coordinated release.
- Root cause (verifier analysis): reflex-base 0.9.9 intentionally deprecated the console.* logging API (console.py _shim_deprecation -> deprecate(deprecation_version="0.9.9")), and the latest published reflex-enterprise (0.9.4, unbounded reflex>=0.9.6 dependency) still calls console.error on its login-gate (_check_login, app.py:120) and prod-tier-gate (check_prod_mode_in_tier, utils.py:119) paths, so the deprecation warning — correctly attributed to the enterprise call sites — surfaces to end users who cannot act on it. Needs a coordinated reflex-enterprise release migrating console.* to stdlib logging (or a base-side exemption for first-party callers).
- Verification notes: CONFIRMED — reflex-enterprise incompatibility (cosmetic, low severity), not a reflex 0.9.9a1 defect and not a functional regression. Independently reproduced with a minimal 3-line rxe.App (not the demos) in clean PyPI-only venvs (reflex-enterprise 0.9.4 + reflex 0.9.9a1 vs 0.9.8): (1) dev run without CI/REFLEX_ACCESS_TOKEN prints 'DeprecationWarning: console.error has been deprecated in version 0.9.9 ... (reflex_enterprise/app.py:120)' immediately before the login-gate exit; (2) 'reflex run --env prod' with CI=1 prints the same warning at reflex_enterprise/utils.py:119 before the paid-subscription gate exit; (3+4) identical runs on 0.9.8 show both gate messages with NO DeprecationWarning. Ne

## Phase 2 refuted / reclassified claims (verified NOT actionable for 0.9.9a1)

- **form-designer example: entry/preview page (/form/<id>) crashes into error boundary — 'FormMessage must be used within FormField or specify the name prop' (pre-existing on 0.9.8 stable, example-app breakage, not a 0.9.9a1 regression)** (`up_lorem_form`): Claim REPRODUCED exactly as described, but REFUTED as an actionable reflex 0.9.9a1 / reflex-enterprise defect — it is a pre-existing app bug in the upstream reflex-examples form-designer example, identical on 0.9.8 stable, so nothing for a fix-agent to act on in this release.

Independent repro (fresh PyPI-only venvs, fresh app copies with no .web/

- **Transient 'incorrect peer dependency react-router@7.18.2' warnings during in-place 0.9.8->0.9.9a1 migration install** (`up_counter_todo`): Observation REPRODUCED exactly but REFUTED as an actionable defect — benign by design, no fix warranted. Independent repro (counter app, fresh dirs, PyPI-only, ports 3608/8608): (1) fresh venv reflex==0.9.8, run to frontend 200; (2) kill, in-place `uv pip install --prerelease=allow -U reflex==0.9.9a1` keeping .web/ + reflex.lock/ (react-router pinn

- **upload example: Files list never refreshes for an existing session (dependency-less cached @rx.var) — pre-existing app-level issue, identical on 0.9.8** (`up_upload_clock`): Claim REPRODUCED exactly as described, but it is NOT a defect a fix-agent should act on: not a reflex 0.9.9a1 defect, not a reflex-enterprise incompatibility, and not a regression (identical on 0.9.8). Independent repro with my own Playwright driver (/tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_up_uplo

- **reflexle example discards its 'Invalid word.' / 'You already guessed this word.' toasts (example-app logic, identical on 0.9.8 and 0.9.9a1)** (`up_reflexle_snake`): Claim VERIFIED as factually accurate, but confirmed=false because it is not a defect a fix-agent should act on: it is example-app logic in reflex-examples' reflexle, pre-existing and identical on 0.9.8 and 0.9.9a1 — neither a reflex 0.9.9a1 defect nor a reflex-enterprise incompatibility, and not a regression. Independent repro (fresh PyPI venvs, fr

- **Fresh prerelease-enabled install of reflex[db] 0.9.9a1 pulls pydantic 2.14.0b1 (beta)** (`up_lorem_form`): REPRODUCED but REFUTED as a defect. The dry-run does resolve pydantic==2.14.0b1 under a global prerelease flag, but: (1) the identical command against STABLE reflex[db]==0.9.8 also pulls pydantic 2.14.0b1, so nothing 0.9.9a1 changed; (2) the premise that alpha adopters must use --pre/--prerelease=allow is false — the published reflex 0.9.9a1 wheel 

- **Transient bun warnings 'incorrect peer dependency react-router@7.18.2' during the one-time 0.9.8->0.9.9a1 package migration** (`up_local_basic`): Claim is factually accurate and independently reproduced, but does not meet the bar of a defect a fix-agent should act on. Repro (minimal blank-template app, fresh 3.11 venv from PyPI, $SB/apps/verify_up_local_basic_1, FP 3764/BP 8764): reflex==0.9.8 init+run to frontend 200 (react-router family 7.18.2 in .web/package.json), in-place upgrade to 0.9

- **basic_crud pre-existing app bug: GET /products/<missing id> returns HTTP 200 with a serialized HTTPException body instead of a 404** (`up_local_basic`): Claim is ACCURATE but correctly classified: not a reflex 0.9.9a1 defect, not a reflex-enterprise incompatibility, and not a regression — confirmed=false because no fix-agent action on reflex is warranted. Independently reproduced in /tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-8501871b3811/scratchpad/apps/verify_up_local_basic_2/: (1) re

- **Coverage gap: enterprise demos cannot be tested in prod mode without a paid Reflex subscription (both 0.9.8 and 0.9.9a1)** (`ent_map_dnd`): Claim's FACTS are accurate but this is NOT a defect/regression a fix-agent should act on — it is intentional reflex-enterprise licensing behavior, byte-identical on 0.9.8, and the claimed coverage gap is escapable via a sanctioned path. Independently reproduced in fresh PyPI-only venvs (/tmp/claude-0/-home-user-reflex/20b6ffc8-8244-5bac-b158-850187

- **MCP plugin backend startup emits pydantic_settings IncompleteFieldDefinitionWarning (Field 'lifespan')** (`ent_misc`): Warning REPRODUCED but REFUTED as a reflex 0.9.9a1 defect or reflex-enterprise incompatibility — no fix-agent action warranted. Independent repro in a fresh venv (reflex==0.9.9a1 + reflex-enterprise[mcp]==0.9.4 from PyPI, stable pydantic 2.13.4 / pydantic-settings 2.15.0 / mcp 1.29.0): the IncompleteFieldDefinitionWarning about Field 'lifespan' app


# Phase 2 cluster summaries (5 upgrade + 3 enterprise clusters / 30 agents)

### `up_counter_todo` (anomaly:3, pass:10)

Upgrade-tested reflex-examples counter and todo apps 0.9.8 -> 0.9.9a1 (in-place venv upgrade preserving .web/ and reflex.lock/, plus cold fresh-.web runs). NO functional regressions: all Playwright-driven user flows (counter: increment/decrement/randomize/color-mode/reload-rehydrate; todo: add via button+Enter, empty-submit no-op, special-char items, check-off/delete, reload persistence) pass identically on both versions, with pixel-identical (md5-equal) screenshots, identical console output, no failed requests, and identical websocket open/close patterns. Migration path verified: lockfile restored from reflex.lock/, react-router-dom pruned via `bun remove` (Removed: 1, zero stale 7.18.2 entries in final bun.lock), react-router family 7.18.2->8.3.0, vite 8.0.16->8.2.0, postcss 8.5.23 override unchanged; cold run converges to the same package.json. Three low-severity log anomalies: backslash-escaped brackets leaking into console warning text (cosmetic regression vs 0.9.8), a new vite configLoader:'native' warning from the template's extension-less vite-plugin-safari-cachebust import, and transient incorrect-peer-dependency warnings during the migration's staged installs.

### `up_upload_clock` (anomaly:3, pass:11)

Upgrade-tested reflex-examples upload and clock apps 0.9.8 -> 0.9.9a1 (in-place venv upgrade preserving .web/reflex.lock, plus cold fresh-.web run), driving all core user flows in real Chromium. NO REGRESSIONS: every check produced identical results on 0.9.8, upgraded 0.9.9a1, and cold 0.9.9a1. Migration path worked cleanly on both apps: lockfiles restored from the app-root reflex.lock/ dir (created by 0.9.8), react-router-dom pruned from package.json via `bun remove` (Removed: 1), react-router family 7.18.2 -> 8.3.0, vite 8.0.16 -> 8.2.0, postcss 8.5.23 override retained; cold-run package.json byte-identical to the migrated one. Upload flows (unicode/space filenames, throttled-upload cancel via rx.cancel_upload, progress events, /_upload serving, pre-upgrade files still served post-upgrade) and clock flows (background tick task, radix select timezone correctness vs real UTC offsets, rx.Cookie persistence, added /moment page proving stable reflex-components-moment 0.9.3 works with the alpha core incl. tz switching) all pass. React Router "Future Flag" warnings from 0.9.8 are gone on 0.9.9a1. Low-severity observations only: new Vite 8.2.0 configLoader-native warning about the extensionless vite-plugin-safari-cachebust import in the reflex template, transient bun peer-dep warnings during the staged migration install, and `reflex run` ignoring SIGTERM/SIGINT when backgrounded (not

### `up_reflexle_snake` (pass:12)

Upgrade-tested reflexle (wordle clone using third-party reflex-global-hotkey) and snakegame (background-loop arrow-key game) from reflex-examples on the 0.9.8 -> 0.9.9a1 in-place migration path plus a cold fresh-.web install. NO REGRESSIONS: all Playwright checks pass on all six runs (reflexle 17 checks x 3 runs, snakegame 19 x 3). The in-place migration cleanly restored reflex.lock, pruned react-router-dom via `bun remove`, bumped react-router 7.18.2->8.3.0 and vite 8.0.16->8.2.0, kept the postcss 8.5.23 override, and produced no new server-log warnings, console errors, or failed requests versus the 0.9.8 baseline. Cold install reused reflex.lock (Install Frontend Packages: 0.18s) and also passed everything. One pre-existing example-app quirk recorded (reflexle discards its invalid-word toast on both versions). All artifacts (NOTES.md, drivers, package.json diffs, debug logs, screenshots) copied to the repo; all spawned servers/browsers killed.

### `up_lorem_form` (anomaly:2, pass:11)

Upgrade-tested lorem-stream (background/yield-heavy streaming) and form-designer (reflex[db] + reflex-local-auth) from reflex-examples on 0.9.8 -> 0.9.9a1. NO REGRESSIONS: in-place migration (reflex.lock/ creation+restore, react-router-dom pruning, react-router 7.18.2->8.3.0, vite 8.0.16->8.2.0) ran cleanly first try on both apps; all browser-driven flows (concurrent stream start/stop/pause/resume/kill; register/login/form+field creation/logout/re-login) behave identically before and after, including cold .web rebuilds and lorem-stream prod mode. 0.9.8-created sqlite data (bcrypt user, forms, fields) is fully usable on 0.9.9a1. form-designer has 3 pre-existing app-level breakages that reproduce identically on 0.9.8 stable (entry-page FormMessage crash, no login auto-redirect, 5 console errors) — recorded, not regressions. Two low-severity notes: fresh prerelease-enabled installs resolve pydantic 2.14.0b1 (beta) via the db extra, and the reflex-generated vite.config.js triggers a new vite 8.2 configLoader:'native' deprecation warning.

### `up_local_basic` (anomaly:1, pass:12)

Upgrade-tested reflex-examples local-component (local JSX asset component via rx.asset shared=True) and basic_crud (reflex[db] + FastAPI api_transformer) from 0.9.8 to 0.9.9a1. No regressions: all 16 end-to-end flows (5 browser flows for local-component; 5 UI CRUD + 6 direct httpx API flows for basic_crud) pass identically on 0.9.8 baseline, after in-place upgrade (preserving .web/ and reflex.lock/), and on a cold 0.9.9a1 run with .web deleted. First-run migration was clean in both apps: lockfiles restored from reflex.lock/, react-router-dom pruned from .web/package.json (bun remove, Removed: 1), react-router/@react-router/* 7.18.2 -> 8.3.0, vite 8.0.16 -> 8.2.0, postcss/postcss-import installed; cold runs reuse reflex.lock via --frozen-lockfile with no changes. Neither app declares react-router-dom in its own source. Anomalies (cosmetic): NEW vite configLoader:'native' warning on every 0.9.9a1 dev start caused by reflex's generated vite.config.js importing ./vite-plugin-safari-cachebust without a file extension; transient "incorrect peer dependency react-router@7.18.2" bun warnings during the one-time migration; pre-existing basic_crud app quirk where GET /products/<missing> returns 200 with a serialized HTTPException body (identical on both versions, app bug not reflex). RR7 Future Flag Warning spam from 0.9.8 is gone on 0.9.9a1.

### `ent_aggrid` (anomaly:3, fail:3, pass:8, skipped:1)

Flagship reflex-enterprise ag_grid demo (rxe 0.9.4) vs reflex 0.9.9a1, end-to-end with Playwright/Chromium, with a full reflex 0.9.8 baseline. CONFIRMED FINDING-001 end-to-end and sharpened it: the whole demo fails to compile on 0.9.9a1, but the user-visible error is NOT the bundled_libraries AttributeError — it is masked into 'VarAttributeError: Attribute _cached_get_all_var_data not found' by reflex_base's custom cached_property/__getattr__ fallback (no chained cause). Exactly 1 of 17 demo pages (/formatters) contains the crashing constructs: python-callable cell_renderer/value_formatter whose compiled return expr carries imports (lambda returning rx.text/rx.tooltip, @rx.memo renderer, formatter using Var dict .get). Minimal trigger: one column def {'cell_renderer': lambda params: rx.text(params.value)} in an otherwise-trivial rxe app — compiles+runs E2E on 0.9.8, crashes compile on 0.9.9a1 (regression). A trivial rxe.App with one plain rxe.ag_grid works fine on 0.9.9a1 E2E, and with the formatters page disabled the other 16 demo routes (editing incl. on_cell_value_changed, sorting, master-detail, tree, selection, pivot, charts, grid-state serialization, state-driven defs) behave identically to 0.9.8. Also found: the shipped demo is broken on reflex 0.9.8 anyway (bundle_library('$/utils/components') is a stale pre-0.9.8 memo path; fixed by bundling '$/app_components/ag_grid/f

### `ent_map_dnd` (anomaly:1, fail:1, pass:10, skipped:1)

reflex-enterprise 0.9.4 map and dnd demos both work END-TO-END on reflex 0.9.9a1, identically to the 0.9.8 baseline (map: 13/13 Playwright checks incl. controls placement, markers/tooltips/popups, wheel-zoom state sync, lambda on_click set_view, pan, MapAPI fly_to/locate with mocked geolocation, vector layers, get_bounds callback; dnd: 18/18 incl. real mouse-down/move/up HTML5 drags on /basic, /foreach and the kanban board with is_over highlight, can_drop previews, column reorder and localStorage persistence). FINDING-001 does NOT fire for the shipped demos — map has no LambdaVar-typed props, and the dnd demo's can_drop functions are @rxe.static which skips LiteralLambdaVar._validate_and_extend_return_expr entirely; a state-var lambda trips the hooks guard (vars.py:138 ValueError) before line 143 on BOTH versions. The crash is real one step off the demo path: a plain (non-static) can_drop/can_drag lambda whose return expr carries imports via the supported bundle_library() pattern hits AttributeError at reflex_enterprise/vars.py:143 on 0.9.9a1 and works on 0.9.8 (minimal repro included) — high-severity regression for the dnd/LambdaVar surface. Caveats: dev-mode enterprise requires CI=1 (login gate exits otherwise); prod mode is untestable on either version (enterprise paid-subscription gate, not circumvented); 0.9.9a1 prod also newly requires frontend-port == backend-port. OSM t

### `ent_misc` (anomaly:3, fail:1, pass:7, skipped:1)

Tested reflex-enterprise 0.9.4 (PyPI) demos mantine + flow, the MCP plugin, and the OIDC demo against reflex 0.9.9a1, end-to-end (real servers, Chromium/Playwright, real MCP client), with 0.9.8 baselines where anything failed. Note: rxe.App() requires login; all runs used CI=true to bypass. RESULTS: (1) mantine demo fully works on 0.9.9a1 — 11/11 browser checks (dates pickers + toasts, pills + remove, tags round-trip, tabs, dropdown nav), zero console/network errors. (2) flow demo REGRESSION: crashes at import on 0.9.9a1 because PR #6382 removed `reflex.page.DECORATED_PAGES` (moved to RegistrationContext.decorated_pages, not mentioned in the 6382 breaking-change note; error is a confusing "cannot import name 'DECORATED_PAGES' from 'PageNamespace' (unknown location)"); the same unmodified demo runs 11/11 on 0.9.8. After a documented 2-line fallback patch, everything else in the flow demo works on 0.9.9a1: node drag with state round-trip, custom nodes, drag-handle, connection-limit enforcement, add-node-on-edge-drop, intersections — 11/11, clean console. (3) MCP plugin (reflex-enterprise[mcp]) fully works on 0.9.9a1: 17/17 — anonymous token endpoint, 401 without bearer, initialize with generated instructions, custom configure= tool/resource, search_events, queue_event with per-token session persistence (5→10), live/computed state var resources, @rxe.mcp.resource. (4) OIDC demo wo


## Environment quirks (benign here, noted for context)

- `reflex init` probes https://registry.npmmirror.com and logs 3x "Failed to connect" before
  falling back (proxy blocks that host). Cosmetic in this environment.
- Vite warns: `configLoader: 'native'` unsupported feature — `import "./vite-plugin-safari-cachebust"
  without a file extension (vite.config.js:4:35)`; suggests adding extension. Framework-owned
  file; will become a hard incompatibility in a future Vite major per warning text. Present in
  dev runs on 0.9.9a1 (vite 8.2.0).


## 0.9.9a2 pyi packaging audit (wheels + sdists, all 14 packages)

Audited the published artifacts of reflex 0.9.9a2, reflex-base 0.9.9a2,
reflex-components-core 0.9.9a2, and the current versions of every other
reflex-components-* package plus gridjs (28 artifacts total). Result: PASS.

- Every package ships its own generated `.pyi` stubs in BOTH the wheel and the
  sdist, and the stub content is byte-identical between the two.
- Zero foreign stubs: no artifact contains `.pyi` files belonging to another
  package.
- Per-package stub counts exactly match the monorepo `pyi_hashes.json` manifest
  (122 stubs across the train: reflex 3, core 36, radix 65, recharts 6,
  react-player 3, code 2, and 1 each for dataeditor/gridjs/lucide/markdown/
  moment/plotly/sonner). reflex-base legitimately ships none (no entries in the
  manifest).
- Building from the sdist works in an isolated env: `hatch-reflex-pyi` (the
  build hook that generates stubs) is published on PyPI, and an sdist install
  lands the same stubs.
- Note (not a defect): shipped stub content hashes differ from the dev-workspace
  `pyi_hashes.json` values because the hook regenerates stubs at build time in
  the release environment; the same divergence exists in the a1 artifacts, and
  wheel/sdist of each release run agree with each other.

Rerun: scripts inline in session history; artifacts fetched from
`https://pypi.org/pypi/<pkg>/<ver>/json` urls, inspected with zipfile/tarfile,
hashes vs `git show origin/main:pyi_hashes.json`.

## 0.9.9a2 re-verification (enterprise)

2026-08-28, fresh venv (`uv venv --python 3.11`), PyPI-only: `reflex==0.9.9a2`
(pulls reflex-base 0.9.9a2) + `reflex-enterprise==0.9.4`. Provenance asserted at the start
of every run (`reflex.__file__`/`reflex_enterprise.__file__` inside the venv; all python
invocations from neutral dirs, never cwd=/home/user/reflex[-enterprise]). Working dir
`$SB/apps/reverify_ent/`; ports 3202-3205/8202-8205; one server at a time, all killed and
ports verified free afterwards. Artifacts: `reverify_a2/` (scripts, logs, screenshots,
NOTES.md with rerun commands).

Verdict: **the a2 shims (PR #6967 `bundled_libraries`, PR #6985 `DECORATED_PAGES` +
`get_config(reload=True)`) fully restore reflex-enterprise 0.9.4 compatibility.**
FINDING-021 (CRITICAL), FINDING-022 (HIGH) and FINDING-023 (HIGH) are all FIXED on
0.9.9a2; the mantine regression sweep shows no new a1->a2 breakage. Each shim emits a
clear, correctly-attributed DeprecationWarning exactly once per process (deduped) — no
noise problem.

| Check | 0.9.9a1 | 0.9.9a2 | Evidence |
| --- | --- | --- | --- |
| FINDING-021 minimal e2e: `REPRO_LAMBDA=1` rxe.ag_grid app (lambda `cell_renderer`) | compile crash (`VarAttributeError`) | **FIXED** — compiles, serves; Chromium: 3 lambda-rendered `<p>` cells, tomato rgb(255,99,71), sort works, 0 console/page errors; pixel-equal to the 0.9.8 baseline shot | `reverify_a2/drive_minimal_a2.py` -> RESULT PASS; `logs/minimal_lambda_a2.log` (0 tracebacks); `shots/min_lambda_a2.png` vs `ent_aggrid/shots_minimal/min_lambda_098.png` |
| FINDING-021 demo: fixed ag_grid demo (`ent_aggrid/demo_098` copy, formatters ENABLED) `/formatters` | whole-app compile dead | **FIXED** — compiles with formatters page; `/` (17 links) + `/formatters` driver report action-for-action identical to the 0.9.8 baseline (5 rows, State/API/Inline tabs, memo `row_counter` button -> `'1\n(00:00)'`, sort), 0 errors | `shots/shots_agdemo_a2/report.json` == `ent_aggrid/shots_098/report.json` for those routes; `formatters.png` shows flag/currency/reversed formatters rendered |
| FINDING-022 dnd non-static `can_drop` + `bundle_library` trigger | exit 1, AttributeError at `reflex_enterprise/vars.py:143` | **FIXED** — exit 0, `DropTarget` created; one deprecation warning | `ent_map_dnd/repro_finding001_dnd_can_drop.py` on a2 venv -> `reverify_a2/repro_dnd_can_drop_a2.out` |
| FINDING-023 flow demo UNMODIFIED (`from reflex.page import DECORATED_PAGES`) | ImportError at flow.py:2, worker dead | **FIXED** — imports/compiles unmodified (byte-identical to `/home/user/reflex-enterprise/demos/flow`); index lists all 6 pages via DECORATED_PAGES; 11/11 Playwright checks incl. `/overview` node drag round-trip + toolbar emoji, connection-limit, edge-drop node creation; 0 console/page errors, 0 4xx/5xx | `ent_misc/drive_flow.py` -> 11/11 PASS; `reverify_a2/logs/flow_a2.log`; `shots/shots_flow_a2/` |
| Regression sweep: mantine demo (worked on a1) | 11/11 | **still 11/11** — dates calendar/DatePicker/TimeInput toasts, pills, tags round-trips, dropdown nav; clean console | `ent_misc/drive_mantine.py` -> 11/11 PASS; `reverify_a2/logs/mantine_a2.log` |
| Offline shim probes | AttributeError / ImportError / TypeError | **all PASS**: `dynamic.bundled_libraries` readable (list); `from reflex.page import DECORATED_PAGES` -> defaultdict; bare `LiteralLambdaVar.create` now reaches rxe's DESIGNED "Library @radix-ui/themes is not bundled" ValueError (matching 0.9.8 behavior outside compile context, per `ent_aggrid` verification pt 4) and creates fine after `bundle_library()`; bonus: `get_config(reload=True)` works again (FINDING-008 half-fixed at the API surface) | `reverify_a2/probe_shims_a2.py` / `.out` |

Deprecation-warning noise (per `--loglevel debug` server log, shim warnings deduped per
process; 2 = compile process + backend worker): minimal app 1x `bundled_libraries`;
ag_grid demo 2x `bundled_libraries`; flow demo 2x `DECORATED_PAGES`. Everything else in
the logs is pre-existing demo-hygiene noise already present on a1/0.9.8 (`@rx.memo`
without annotations x10, `App(theme=...)`, `ArrayVar.foreach`, string `disable_plugins`,
implicit Radix Themes, `console.error/info` from rxe internals, `reflex.Model`).

Not re-tested (unchanged expectations): FINDING-024 (AttributeError masking — pre-existing,
orthogonal), FINDING-025 (shipped ag_grid demo's stale `$/utils/components` bundle path —
broken on 0.9.8 too; the a2 runs used the fixed `demo_098` copy), FINDING-026 (cookies/sync
404 — enterprise-side, both versions), FINDING-029 (console.* deprecation on rxe gate
paths — still expected, observed benignly in the agdemo log).
